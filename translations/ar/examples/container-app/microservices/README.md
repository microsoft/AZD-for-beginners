# هندسة الخدمات المصغرة - مثال على تطبيق الحاويات

⏱️ **الوقت المقدر**: 25-35 دقيقة | 💰 **التكلفة المقدرة**: ~$50-100/شهريًا | ⭐ **التعقيد**: متقدم

هندسة خدمات مصغرة **مبسطة لكنها عملية** مُنشَرة على Azure Container Apps باستخدام AZD CLI. يوضح هذا المثال التواصل بين الخدمات، تنسيق الحاويات، والمراقبة من خلال إعداد عملي يتكون من خدمتين.

> **📚 منهج التعلم**: يبدأ هذا المثال ببنية بسيطة مكونة من خدمتين (بوابة API + خدمة خلفية) يمكنك نشرها فعليًا والتعلم منها. بعد إتقان هذا الأساس، نقدم إرشادات لتوسيعها إلى نظام خدمات مصغرة كامل.

## ما ستتعلم

بإكمال هذا المثال، ستتمكن من:
- نشر حاويات متعددة على Azure Container Apps
- تنفيذ التواصل بين الخدمات عبر الشبكات الداخلية
- تكوين القياسات التكيفية والصحة بناءً على البيئة
- مراقبة التطبيقات الموزعة باستخدام Application Insights
- فهم أنماط نشر الخدمات المصغرة وأفضل الممارسات
- تعلم التوسع التدريجي من بنية بسيطة إلى معقدة

## البنية

### المرحلة 1: ما سنبنيه (مضمن في هذا المثال)

```
                    ┌─────────────────────────────┐
                    │         Internet            │
                    └──────────────┬──────────────┘
                                   │
                                   │ HTTPS
                                   │
                    ┌──────────────▼──────────────┐
                    │      API Gateway            │
                    │   (Node.js Container)       │
                    │   - Routes requests         │
                    │   - Health checks           │
                    │   - Request logging         │
                    └──────────────┬──────────────┘
                                   │
                                   │ HTTP (internal)
                                   │
                    ┌──────────────▼──────────────┐
                    │    Product Service          │
                    │   (Python Container)        │
                    │   - Product CRUD            │
                    │   - In-memory data store    │
                    │   - REST API                │
                    └──────────────┬──────────────┘
                                   │
                    ┌──────────────▼──────────────┐
                    │   Application Insights      │
                    │   (Monitoring & Logs)       │
                    └─────────────────────────────┘
```

**لماذا نبدأ بالبساطة؟**
- ✅ النشر والفهم بسرعة (25-35 دقيقة)
- ✅ تعلم أنماط الخدمات المصغرة الأساسية بدون تعقيد
- ✅ كود عملي يمكنك تعديله والتجربة عليه
- ✅ تكلفة أقل للتعلم (~$50-100/شهريًا مقابل $300-1400/شهريًا)
- ✅ بناء ثقة قبل إضافة قواعد بيانات وقوائم رسائل

**تشبيه**: فكر في الأمر مثل تعلم القيادة. تبدأ بموقف سيارات فارغ (خدمتان)، تتقن الأساسيات، ثم تتقدم إلى حركة المرور في المدينة (5+ خدمات مع قواعد بيانات).

### المرحلة 2: التوسع المستقبلي (بنية مرجعية)

بمجرد إتقان بنية الخدمتين، يمكنك التوسع إلى:

```
Full Architecture (Not Included - For Reference)
├── API Gateway (✅ Included)
├── Product Service (✅ Included)
├── Order Service (🔜 Add next)
├── User Service (🔜 Add next)
├── Notification Service (🔜 Add last)
├── Azure Service Bus (🔜 For async communication)
├── Cosmos DB (🔜 For product persistence)
├── Azure SQL (🔜 For order management)
└── Azure Storage (🔜 For file storage)
```

راجع قسم "دليل التوسع" في النهاية للحصول على إرشادات خطوة بخطوة.

## الميزات المضمنة

✅ **اكتشاف الخدمات**: اكتشاف تلقائي معتمد على DNS بين الحاويات  
✅ **توزيع التحميل**: موازنة تحميل مدمجة عبر النسخ المكررة  
✅ **التوسيع التلقائي**: توسيع مستقل لكل خدمة بناءً على طلبات HTTP  
✅ **مراقبة الصحة**: فحوصات liveness و readiness لكلتا الخدمتين  
✅ **تسجيل موزع**: تسجيل مركزي باستخدام Application Insights  
✅ **شبكات داخلية**: اتصال آمن بين الخدمات  
✅ **تنسيق الحاويات**: نشر وتوسيع تلقائي للحاويات  
✅ **تحديثات بدون توقف**: تحديثات متدحرجة مع إدارة الإصدارات  

## المتطلبات المسبقة

### الأدوات المطلوبة

قبل البدء، تأكد من تثبيت الأدوات التالية:

1. **[Azure Developer CLI (azd)](https://learn.microsoft.com/azure/developer/azure-developer-cli/install-azd)** (الإصدار 1.0.0 أو أعلى)
   ```bash
   azd version
   # الإخراج المتوقع: إصدار azd 1.0.0 أو أعلى
   ```

2. **[Azure CLI](https://learn.microsoft.com/cli/azure/install-azure-cli)** (الإصدار 2.50.0 أو أعلى)
   ```bash
   az --version
   # الإخراج المتوقع: azure-cli 2.50.0 أو أعلى
   ```

3. **[Docker](https://www.docker.com/get-started)** (للتطوير/الاختبار المحلي - اختياري)
   ```bash
   docker --version
   # المخرجات المتوقعة: إصدار Docker 20.10 أو أحدث
   ```

### متطلبات Azure

- اشتراك **Azure نشط** ([create a free account](https://azure.microsoft.com/free/))
- أذونات لإنشاء الموارد في الاشتراك الخاص بك
- دور **Contributor** على الاشتراك أو مجموعة الموارد

### المعارف المطلوبة

هذا مثال على مستوى **متقدم**. يجب أن تكون قد:
- أكملت مثال [Simple Flask API example](../../../../../examples/container-app/simple-flask-api) 
- فهم أساسي لهندسة الخدمات المصغرة
- إلمام بـ REST APIs و HTTP
- فهم مفاهيم الحاويات

**جديد على Container Apps؟** ابدأ بمثال [Simple Flask API example](../../../../../examples/container-app/simple-flask-api) أولاً لتعلم الأساسيات.

## بداية سريعة (خطوة بخطوة)

### الخطوة 1: استنساخ التنقل

```bash
git clone https://github.com/microsoft/AZD-for-beginners.git
cd AZD-for-beginners/examples/container-app/microservices
```

**✓ فحص النجاح**: تحقق من رؤية `azure.yaml`:
```bash
ls
# متوقع: README.md, azure.yaml, infra/, src/
```

### الخطوة 2: المصادقة مع Azure

```bash
azd auth login
```

سيُفتح متصفحك للمصادقة على Azure. سجّل الدخول باستخدام بيانات اعتماد Azure الخاصة بك.

**✓ فحص النجاح**: يجب أن ترى:
```
Logged in to Azure.
```

### الخطوة 3: تهيئة البيئة

```bash
azd init
```

**المطالبات التي سترى**:
- **اسم البيئة**: أدخل اسمًا قصيرًا (مثال: `microservices-dev`)
- **اشتراك Azure**: اختر الاشتراك الخاص بك
- **منطقة Azure**: اختر منطقة (مثال: `eastus`, `westeurope`)

**✓ فحص النجاح**: يجب أن ترى:
```
SUCCESS: New project initialized!
```

### الخطوة 4: نشر البنية التحتية والخدمات

```bash
azd up
```

**ما الذي يحدث** (يستغرق 8-12 دقيقة):
1. ينشئ بيئة Container Apps
2. ينشئ Application Insights للمراقبة
3. يبني حاوية بوابة API (Node.js)
4. يبني حاوية خدمة المنتج (Python)
5. ينشر كلتا الحاويتين إلى Azure
6. يكوّن الشبكات وفحوصات الصحة
7. يهيئ المراقبة والتسجيل

**✓ فحص النجاح**: يجب أن ترى:
```
SUCCESS: Your application was deployed to Azure in X minutes Y seconds.
Endpoint: https://api-gateway-<unique-id>.azurecontainerapps.io
```

**⏱️ الوقت**: 8-12 دقيقة

### الخطوة 5: اختبار النشر

```bash
# احصل على نقطة نهاية البوابة
GATEWAY_URL=$(azd env get-values | grep API_GATEWAY_URL | cut -d '=' -f2 | tr -d '"')

# اختبار صحة بوابة واجهات برمجة التطبيقات
curl $GATEWAY_URL/health

# الإخراج المتوقع:
# {"status":"سليم","service":"بوابة واجهات برمجة التطبيقات","timestamp":"2025-11-19T10:30:00Z"}
```

**اختبر خدمة المنتج عبر البوابة**:
```bash
# قائمة المنتجات
curl $GATEWAY_URL/api/products

# الناتج المتوقع:
# [
#   {"id":1,"name":"Laptop","price":999.99,"stock":50},
#   {"id":2,"name":"Mouse","price":29.99,"stock":200},
#   {"id":3,"name":"Keyboard","price":79.99,"stock":150}
# ]
```

**✓ فحص النجاح**: كلا الطرفين يعيدان بيانات JSON بدون أخطاء.

---

**🎉 تهانينا!** لقد نشرت بنية خدمات مصغرة على Azure!

## هيكل المشروع

جميع ملفات التنفيذ مضمنة—هذا مثال كامل وعملي:

```
microservices/
│
├── README.md                         # This file
├── azure.yaml                        # AZD configuration
├── .gitignore                        # Git ignore patterns
│
├── infra/                           # Infrastructure as Code (Bicep)
│   ├── main.bicep                   # Main orchestration
│   ├── abbreviations.json           # Naming conventions
│   ├── core/                        # Shared infrastructure
│   │   ├── container-apps-environment.bicep  # Container environment + registry
│   │   └── monitor.bicep            # Application Insights + Log Analytics
│   └── app/                         # Service definitions
│       ├── api-gateway.bicep        # API Gateway container app
│       └── product-service.bicep    # Product Service container app
│
└── src/                             # Application source code
    ├── api-gateway/                 # Node.js API Gateway
    │   ├── app.js                   # Express server with routing
    │   ├── package.json             # Node dependencies
    │   └── Dockerfile               # Container definition
    └── product-service/             # Python Product Service
        ├── main.py                  # Flask API with product data
        ├── requirements.txt         # Python dependencies
        └── Dockerfile               # Container definition
```

**ما الذي تفعله كل مكوّنات:**

**Infrastructure (infra/)**:
- `main.bicep`: ينظم جميع موارد Azure واعتمادياتها
- `core/container-apps-environment.bicep`: ينشئ بيئة Container Apps و Azure Container Registry
- `core/monitor.bicep`: يهيئ Application Insights للتسجيل الموزع
- `app/*.bicep`: تعريفات تطبيقات الحاوية الفردية مع التوسيع وفحوصات الصحة

**API Gateway (src/api-gateway/)**:
- خدمة وجهة عامة تقوم بتوجيه الطلبات إلى الخدمات الخلفية
- تنفّذ التسجيل، معالجة الأخطاء، وإعادة توجيه الطلبات
- توضح التواصل بين الخدمات عبر HTTP

**Product Service (src/product-service/)**:
- خدمة داخلية تحتوي على كتالوج المنتجات (في الذاكرة للبساطة)
- واجهة REST مع فحوصات الصحة
- مثال لنمط خدمة خلفية

## لمحة عن الخدمات

### API Gateway (Node.js/Express)

**المنفذ**: 8080  
**الوصول**: عام (ingress خارجي)  
**الغرض**: يوجّه الطلبات الواردة إلى الخدمات الخلفية المناسبة  

**نقاط النهاية**:
- `GET /` - معلومات عن الخدمة
- `GET /health` - نقطة فحص الصحة
- `GET /api/products` - يعيد توجيه إلى خدمة المنتج (قائمة الكل)
- `GET /api/products/:id` - يعيد توجيه إلى خدمة المنتج (احصل بواسطة المعرف)

**الميزات الرئيسية**:
- توجيه الطلبات باستخدام axios
- تسجيل مركزي
- معالجة الأخطاء وإدارة انتهاء المهلة
- اكتشاف الخدمات عبر متغيرات البيئة
- تكامل مع Application Insights

**إبراز الكود** (`src/api-gateway/app.js`):
```javascript
// الاتصال بين الخدمات الداخلية
app.get('/api/products', async (req, res) => {
  const response = await axios.get(`${PRODUCT_SERVICE_URL}/products`);
  res.json(response.data);
});
```

### Product Service (Python/Flask)

**المنفذ**: 8000  
**الوصول**: داخلي فقط (بدون ingress خارجي)  
**الغرض**: يدير كتالوج المنتجات باستخدام بيانات في الذاكرة  

**نقاط النهاية**:
- `GET /` - معلومات عن الخدمة
- `GET /health` - نقطة فحص الصحة
- `GET /products` - عرض جميع المنتجات
- `GET /products/<id>` - الحصول على منتج حسب المعرف

**الميزات الرئيسية**:
- واجهة REST باستخدام Flask
- مخزن منتجات في الذاكرة (بسيط، لا حاجة لقاعدة بيانات)
- مراقبة الصحة باستخدام probes
- تسجيل منظم
- تكامل مع Application Insights

**نموذج البيانات**:
```python
{
  "id": 1,
  "name": "Laptop",
  "description": "High-performance laptop",
  "price": 999.99,
  "stock": 50
}
```

**لماذا داخلي فقط؟**
خدمة المنتج غير مكشوفة للجمهور. يجب أن تمر جميع الطلبات عبر بوابة API، التي توفر:
- الأمان: نقطة وصول مُتحكَّم بها
- المرونة: إمكانية تغيير الخلفية دون التأثير على العملاء
- المراقبة: تسجيل مركزي للطلبات

## فهم تواصل الخدمات

### كيف تتحدث الخدمات مع بعضها

في هذا المثال، تتواصل بوابة API مع خدمة المنتج باستخدام **استدعاءات HTTP داخلية**:

```javascript
// بوابة واجهة برمجة التطبيقات (src/api-gateway/app.js)
const PRODUCT_SERVICE_URL = process.env.PRODUCT_SERVICE_URL;

// إجراء طلب HTTP داخلي
const response = await axios.get(`${PRODUCT_SERVICE_URL}/products`);
```

**النقاط الرئيسية**:

1. **اكتشاف معتمد على DNS**: توفر Container Apps تلقائيًا DNS للخدمات الداخلية
   - FQDN خدمة المنتج: `product-service.internal.<environment>.azurecontainerapps.io`
   - مبسطًا كـ: `http://product-service` (تحله Container Apps)

2. **لا تعريض عام**: خدمة المنتج لديها `external: false` في Bicep
   - متاحة فقط داخل بيئة Container Apps
   - لا يمكن الوصول إليها من الإنترنت

3. **متغيرات البيئة**: تُحقن عناوين الخدمات عند النشر
   - تمرر Bicep الـ FQDN الداخلي إلى البوابة
   - لا عناوين URL مضمنة في الكود التطبيقي

**تشبيه**: فكر في الأمر كغرف مكتب. بوابة API هي مكتب الاستقبال (واجهة عامة)، وخدمة المنتج هي غرفة مكتب (داخلية فقط). يجب على الزائرين المرور عبر الاستقبال للوصول إلى أي مكتب.

## خيارات النشر

### النشر الكامل (موصى به)

```bash
# نشر البنية التحتية والخدمتين
azd up
```

هذا ينشر:
1. بيئة Container Apps
2. Application Insights
3. Container Registry
4. حاوية API Gateway
5. حاوية Product Service

**الوقت**: 8-12 دقيقة

### نشر خدمة فردية

```bash
# انشر خدمة واحدة فقط (بعد تشغيل azd up في البداية)
azd deploy api-gateway

# أو انشر خدمة المنتج
azd deploy product-service
```

**حالة الاستخدام**: عندما تقوم بتحديث الكود في خدمة واحدة وتريد إعادة نشر تلك الخدمة فقط.

### تحديث التكوين

```bash
# تغيير معلمات التحجيم
azd env set GATEWAY_MAX_REPLICAS 30

# إعادة النشر بالتكوين الجديد
azd up
```

## التكوين

### إعدادات التوسيع

تم تكوين كلتا الخدمتين باستخدام التوسيع التلقائي المبني على HTTP في ملفات Bicep الخاصة بهما:

**API Gateway**:
- الحد الأدنى للنسخ: 2 (دائمًا على الأقل 2 للتوافر)
- الحد الأقصى للنسخ: 20
- مُشغل التوسيع: 50 طلب متزامن لكل نسخة

**Product Service**:
- الحد الأدنى للنسخ: 1 (يمكن أن يتوسع إلى صفر إذا لزم الأمر)
- الحد الأقصى للنسخ: 10
- مُشغل التوسيع: 100 طلب متزامن لكل نسخة

**تخصيص التوسيع** (في `infra/app/*.bicep`):
```bicep
scale: {
  minReplicas: 1
  maxReplicas: 10
  rules: [
    {
      name: 'http-scale-rule'
      http: {
        metadata: {
          concurrentRequests: '100'  // Adjust this
        }
      }
    }
  ]
}
```

### تخصيص الموارد

**API Gateway**:
- CPU: 1.0 vCPU
- الذاكرة: 2 GiB
- السبب: يتعامل مع كل الحركة الخارجية

**Product Service**:
- CPU: 0.5 vCPU
- الذاكرة: 1 GiB
- السبب: عمليات في الذاكرة خفيفة الوزن

### فحوصات الصحة

تتضمن كلتا الخدمتين فحوصات liveness و readiness:

```bicep
probes: [
  {
    type: 'Liveness'
    httpGet: {
      path: '/health'
      port: 8080
    }
    initialDelaySeconds: 10
    periodSeconds: 30
  }
  {
    type: 'Readiness'
    httpGet: {
      path: '/health'
      port: 8080
    }
    initialDelaySeconds: 5
    periodSeconds: 10
  }
]
```

**ما معنى هذا**:
- **Liveness**: إذا فشل فحص الصحة، تعيد Container Apps تشغيل الحاوية
- **Readiness**: إذا لم تكن جاهزة، تتوقف Container Apps عن توجيه المرور لتلك النسخة



## المراقبة والرصد

### عرض سجلات الخدمة

```bash
# عرض السجلات باستخدام azd monitor
azd monitor --logs

# أو استخدم Azure CLI لتطبيقات الحاويات المحددة:
# بث السجلات من بوابة API
az containerapp logs show --name api-gateway --resource-group $RG_NAME --follow

# عرض السجلات الأخيرة لخدمة المنتج
az containerapp logs show --name product-service --resource-group $RG_NAME --tail 100
```

**المخرجات المتوقعة**:
```
[api-gateway] API Gateway listening on port 8080
[api-gateway] Product Service URL: http://product-service
[api-gateway] GET /api/products 200 - 45ms
[product-service] Retrieved 5 products
```

### استعلامات Application Insights

افتح Application Insights في بوابة Azure، ثم نفّذ هذه الاستعلامات:

**العثور على الطلبات البطيئة**:
```kusto
requests
| where timestamp > ago(1h)
| where duration > 1000  // Requests taking >1 second
| summarize count() by name, cloud_RoleName
| order by count_ desc
```

**تتبّع استدعاءات خدمة-إلى-خدمة**:
```kusto
dependencies
| where timestamp > ago(1h)
| where type == "Http"
| project timestamp, name, target, duration, success
| order by timestamp desc
```

**معدل الأخطاء حسب الخدمة**:
```kusto
exceptions
| where timestamp > ago(24h)
| summarize errorCount = count() by cloud_RoleName, type
| order by errorCount desc
```

**حجم الطلبات عبر الزمن**:
```kusto
requests
| where timestamp > ago(1h)
| summarize requestCount = count() by bin(timestamp, 5m), cloud_RoleName
| render timechart
```

### الوصول إلى لوحة المراقبة

```bash
# الحصول على تفاصيل Application Insights
azd env get-values | grep APPLICATIONINSIGHTS

# فتح المراقبة في بوابة Azure
az monitor app-insights component show \
  --app $(azd env get-values | grep APPLICATIONINSIGHTS_CONNECTION_STRING | cut -d '=' -f2) \
  --resource-group $(azd env get-values | grep AZURE_RESOURCE_GROUP | cut -d '=' -f2) \
  --query "appId" -o tsv
```

### المقاييس الحية

1. انتقل إلى Application Insights في بوابة Azure
2. انقر على "Live Metrics"
3. شاهد الطلبات في الوقت الحقيقي، والإخفاقات، والأداء
4. اختبر بتشغيل: `curl $(azd env get-values | grep API_GATEWAY_URL | cut -d '=' -f2 | tr -d '"')/api/products`

## تمارين عملية

[ملاحظة: راجع التمارين الكاملة أعلاه في قسم "التمارين العملية" للحصول على تمارين مفصلة خطوة بخطوة تشمل التحقق من النشر، تعديل البيانات، اختبارات التوسيع التلقائي، معالجة الأخطاء، وإضافة خدمة ثالثة.]

## تحليل التكلفة

### التكاليف الشهرية المقدرة (لهذا المثال المكوّن من خدمتين)

| Resource | Configuration | Estimated Cost |
|----------|--------------|----------------|
| API Gateway | 2-20 replicas, 1 vCPU, 2GB RAM | $30-150 |
| Product Service | 1-10 replicas, 0.5 vCPU, 1GB RAM | $15-75 |
| Container Registry | Basic tier | $5 |
| Application Insights | 1-2 GB/month | $5-10 |
| Log Analytics | 1 GB/month | $3 |
| **Total** | | **$58-243/month** |

**تفصيل التكلفة حسب الاستخدام**:
- **حركة خفيفة** (اختبار/تعلم): ~$60/شهر
- **حركة معتدلة** (إنتاج صغير): ~$120/شهر
- **حركة مرتفعة** (فترات ازدحام): ~$240/شهر

### نصائح لتحسين التكلفة

1. **التوسيع إلى الصفر للتطوير**:
   ```bicep
   scale: {
     minReplicas: 0  // Save $30-40/month when not in use
     maxReplicas: 10
   }
   ```

2. **استخدم خطة الاستهلاك لـ Cosmos DB** (عند إضافتها):
   - ادفع فقط مقابل ما تستخدمه
   - لا توجد رسوم دنيا

3. **ضبط أخذ عينات Application Insights**:
   ```javascript
   appInsights.defaultClient.config.samplingPercentage = 50; // عيّن 50٪ من الطلبات
   ```

4. **نظف الموارد عند عدم الحاجة**:
   ```bash
   azd down
   ```

### خيارات الطبقة المجانية
للتعلّم/الاختبار، ضع في الاعتبار:
- استخدام أرصدة Azure المجانية (أول 30 يومًا)
- الحفاظ على الحد الأدنى من النسخ المتماثلة
- الحذف بعد الاختبار (لا توجد رسوم مستمرة)

---

## Cleanup

لتجنب الرسوم المستمرة، احذف كل الموارد:

```bash
azd down --force --purge
```

**موجه التأكيد**:
```
? Total resources to delete: 6, are you sure you want to continue? (y/N)
```

اكتب `y` للتأكيد.

**ما سيتم حذفه**:
- بيئة Container Apps
- كلا تطبيقي Container Apps (gateway و product service)
- Container Registry
- Application Insights
- Log Analytics Workspace
- Resource Group

**✓ التحقق من التنظيف**:
```bash
az group list --query "[?starts_with(name,'rg-microservices')]" --output table
```

يجب أن يُرجِع فارغًا.

---

## Expansion Guide: From 2 to 5+ Services

بمجرد إتقانك لهذا التصميم المكوّن من خدمتين، إليك كيفية التوسع:

### المرحلة 1: إضافة تخزين قاعدة بيانات دائم (الخطوة التالية)

**إضافة Cosmos DB لخدمة المنتج**:

1. أنشئ `infra/core/cosmos.bicep`:
   ```bicep
   resource cosmosAccount 'Microsoft.DocumentDB/databaseAccounts@2023-04-15' = {
     name: name
     location: location
     kind: 'GlobalDocumentDB'
     properties: {
       databaseAccountOfferType: 'Standard'
       locations: [{ locationName: location, failoverPriority: 0 }]
     }
   }
   ```

2. حدّث خدمة المنتج لاستخدام Cosmos DB بدلاً من البيانات المخزنة في الذاكرة

3. التكلفة الإضافية المقدَّرة: ~25$/شهريًا (بدون خادم)

### المرحلة 2: إضافة خدمة ثالثة (إدارة الطلبات)

**إنشاء خدمة الطلبات**:

1. مجلد جديد: `src/order-service/` (Python/Node.js/C#)
2. ملف Bicep جديد: `infra/app/order-service.bicep`
3. حدّث API Gateway ليوجه المسار `/api/orders`
4. أضف Azure SQL Database لتخزين الطلبات الدائم

**يصبح التصميم**:
```
API Gateway → Product Service (Cosmos DB)
           → Order Service (Azure SQL)
```

### المرحلة 3: إضافة تواصل غير متزامن (Service Bus)

**تنفيذ بنية قائمة على الأحداث**:

1. أضف Azure Service Bus: `infra/core/servicebus.bicep`
2. تنشر خدمة المنتج أحداث "ProductCreated"
3. خدمة الطلبات تشترك في أحداث المنتج
4. أضف خدمة الإشعارات لمعالجة الأحداث

**النمط**: طلب/استجابة (HTTP) + قائم على الأحداث (Service Bus)

### المرحلة 4: إضافة مصادقة المستخدمين

**تنفيذ خدمة المستخدم**:

1. أنشئ `src/user-service/` (Go/Node.js)
2. أضف Azure AD B2C أو مصادقة JWT مخصصة
3. يتحقق API Gateway من صحة الرموز
4. تتحقق الخدمات من أذونات المستخدم

### المرحلة 5: جاهزية الإنتاج

**أضف هذه المكونات**:
- Azure Front Door (موازنة تحميل عالمية)
- Azure Key Vault (إدارة الأسرار)
- Azure Monitor Workbooks (لوحات معلومات مخصصة)
- خط أنابيب CI/CD (GitHub Actions)
- عمليات نشر Blue-Green
- Managed Identity لجميع الخدمات

**تكلفة بنية الإنتاج الكاملة**: ~300-1,400$/شهريًا

---

## Learn More

### Related Documentation
- [توثيق Azure Container Apps](https://learn.microsoft.com/azure/container-apps/)
- [دليل هندسة الخدمات المصغرة](https://learn.microsoft.com/azure/architecture/guide/architecture-styles/microservices)
- [Application Insights للتتبع الموزع](https://learn.microsoft.com/azure/azure-monitor/app/distributed-tracing)
- [توثيق Azure Developer CLI](https://learn.microsoft.com/azure/developer/azure-developer-cli/)

### Next Steps in This Course
- ← السابق: [واجهة Flask بسيطة](../../../../../examples/container-app/simple-flask-api) - مثال مبتدئ لحاوية واحدة
- → التالي: [دليل دمج الذكاء الاصطناعي](../../../../../examples/docs/ai-foundry) - أضف قدرات الذكاء الاصطناعي
- 🏠 [الصفحة الرئيسية للدورة](../../README.md)

### Comparison: When to Use What

**تطبيق حاوية واحدة** (مثال واجهة Flask بسيطة):
- ✅ تطبيقات بسيطة
- ✅ بنية أحادية
- ✅ سريع النشر
- ❌ قابلية توسع محدودة
- **التكلفة**: ~15-50$/شهريًا

**الخدمات المصغرة** (هذا المثال):
- ✅ تطبيقات معقدة
- ✅ قابلية التوسع بشكل مستقل لكل خدمة
- ✅ استقلالية الفرق (خدمات مختلفة، فرق مختلفة)
- ❌ أكثر تعقيدًا للإدارة
- **التكلفة**: ~60-250$/شهريًا

**Kubernetes (AKS)**:
- ✅ أقصى درجة من التحكم والمرونة
- ✅ قابلية النقل عبر السحابات المتعددة
- ✅ شبكات متقدمة
- ❌ يتطلب خبرة في Kubernetes
- **التكلفة**: ~150-500$/شهريًا كحد أدنى

**التوصية**: ابدأ بـ Container Apps (هذا المثال)، انتقل إلى AKS فقط إذا كنت بحاجة إلى ميزات خاصة بـ Kubernetes.

---

## Frequently Asked Questions

**Q: لماذا خدمتان فقط بدلًا من 5+؟**  
A: Educational progression. أتقن الأساسيات (الاتصال بين الخدمات، المراقبة، التحجيم) بمثال بسيط قبل إضافة التعقيد. الأنماط التي تتعلمها هنا تنطبق على هياكل مكونة من 100 خدمة.

**Q: هل يمكنني إضافة المزيد من الخدمات بنفسي؟**  
A: بالتأكيد! اتبع دليل التوسع أعلاه. كل خدمة جديدة تتبع نفس النمط: أنشئ مجلد src، أنشئ ملف Bicep، حدّث azure.yaml، ثم انشر.

**Q: هل هذا جاهز للإنتاج؟**  
A: إنه أساس متين. للإنتاج، أضف: managed identity، Key Vault، قواعد بيانات دائمة، خط أنابيب CI/CD، تنبيهات المراقبة، واستراتيجية نسخ احتياطي.

**Q: لماذا لا نستخدم Dapr أو خدمة الشبكة أخرى؟**  
A: أبقِ الأمر بسيطًا للتعلم. بمجرد أن تفهم الشبكات الأصلية لـ Container Apps، يمكنك إضافة Dapr للحالات المتقدمة.

**Q: كيف أقوم بالتصحيح محليًا؟**  
A: شغّل الخدمات محليًا باستخدام Docker:
```bash
cd src/api-gateway
docker build -t local-gateway .
docker run -p 8080:8080 -e PRODUCT_SERVICE_URL=http://localhost:8000 local-gateway
```

**Q: هل يمكنني استخدام لغات برمجة مختلفة؟**  
A: نعم! هذا المثال يوضح Node.js (gateway) + Python (خدمة المنتج). يمكنك مزج أي لغات تعمل داخل الحاويات.

**Q: ماذا لو لم تكن لدي أرصدة Azure؟**  
A: استخدم المستوى المجاني لـ Azure (أول 30 يومًا للحسابات الجديدة) أو انشر لفترات اختبار قصيرة واحذف فورًا.

---

> **🎓 ملخص مسار التعلم**: لقد تعلمت نشر بنية متعددة الخدمات مع التحجيم التلقائي، الشبكات الداخلية، المراقبة المركزية، وأنماط جاهزة للإنتاج. هذا الأساس يجهّزك لأنظمة موزعة معقدة وهندسات الخدمات المصغرة المؤسسية.

**📚 تنقل الدورة:**
- ← السابق: [واجهة Flask بسيطة](../../../../../examples/container-app/simple-flask-api)
- → التالي: [مثال تكامل قاعدة البيانات](../../../../../examples/database-app)
- 🏠 [الصفحة الرئيسية للدورة](../../../README.md)
- 📖 [أفضل ممارسات Container Apps](../../../docs/chapter-04-infrastructure/deployment-guide.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
إخلاء المسؤولية:
تمت ترجمة هذا المستند باستخدام خدمة الترجمة الآلية Co‑op Translator (https://github.com/Azure/co-op-translator). وبينما نسعى لتحقيق الدقة، يُرجى ملاحظة أن الترجمات الآلية قد تحتوي على أخطاء أو عدم دقة. يجب اعتبار الوثيقة الأصلية بلغتها الأصلية المصدر المرجعي والموثوق. للمعلومات الحرجة، يُنصح بالاستعانة بترجمة احترافية من مترجم بشري. نحن لا نتحمل أي مسؤولية عن أي سوء فهم أو تفسير خاطئ ينشأ عن استخدام هذه الترجمة.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->