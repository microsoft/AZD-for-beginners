# معماری میکروسرویس‌ها - مثال Container App

⏱️ **زمان تقریبی**: 25-35 دقیقه | 💰 **هزینه تقریبی**: ~$50-100/month | ⭐ **پیچیدگی**: پیشرفته

یک معماری میکروسرویس ساده اما کاربردی که با استفاده از AZD CLI در Azure Container Apps مستقر شده است. این مثال ارتباط سرویس به سرویس، ارکستراسیون کانتینرها و مانیتورینگ را با یک پیاده‌سازی عملی شامل ۲ سرویس نشان می‌دهد.

> **📚 رویکرد یادگیری**: این مثال با یک معماری حداقلی ۲ سرویس (API Gateway + سرویس بک‌اند) شروع می‌کند که می‌توانید واقعاً مستقر کنید و از آن بیاموزید. پس از تسلط بر این پایه، راهنمایی‌هایی برای گسترش به یک اکوسیستم کامل میکروسرویس ارائه می‌دهیم.

## آنچه یاد خواهید گرفت

با تکمیل این مثال، شما خواهید:
- چند کانتینر را در Azure Container Apps مستقر کنید
- ارتباط سرویس به سرویس را با شبکه‌بندی داخلی پیاده‌سازی کنید
- مقیاس‌پذیری مبتنی بر محیط و بررسی‌های سلامت را پیکربندی کنید
- برنامه‌های توزیع‌شده را با Application Insights مانیتور کنید
- الگوهای استقرار میکروسرویس و بهترین روش‌ها را درک کنید
- یاد بگیرید چگونه به‌تدریج از معماری ساده به معماری پیچیده‌تر گسترش دهید

## معماری

### فاز 1: آنچه می‌سازیم (شامل این مثال)

```
                    ┌─────────────────────────────┐
                    │         Internet            │
                    └──────────────┬──────────────┘
                                   │
                                   │ HTTPS
                                   │
                    ┌──────────────▼──────────────┐
                    │      API Gateway            │
                    │   (Node.js Container)       │
                    │   - Routes requests         │
                    │   - Health checks           │
                    │   - Request logging         │
                    └──────────────┬──────────────┘
                                   │
                                   │ HTTP (internal)
                                   │
                    ┌──────────────▼──────────────┐
                    │    Product Service          │
                    │   (Python Container)        │
                    │   - Product CRUD            │
                    │   - In-memory data store    │
                    │   - REST API                │
                    └──────────────┬──────────────┘
                                   │
                    ┌──────────────▼──────────────┐
                    │   Application Insights      │
                    │   (Monitoring & Logs)       │
                    └─────────────────────────────┘
```

**چرا ساده شروع کنیم؟**
- ✅ سریع مستقر کنید و سریع درک کنید (25-35 دقیقه)
- ✅ الگوهای اصلی میکروسرویس را بدون پیچیدگی بیاموزید
- ✅ کد عملی که می‌توانید آن را تغییر دهید و آزمایش کنید
- ✅ هزینه کمتر برای یادگیری (~$50-100/month در مقابل $300-1400/month)
- ✅ قبل از افزودن پایگاه‌های داده و صف‌های پیام، اعتمادبه‌نفس بسازید

**تشبیه**: این کار مانند یاد گرفتن رانندگی است. با یک پارکینگ خالی (۲ سرویس) شروع می‌کنید، اصول را فرا می‌گیرید، سپس به ترافیک شهری پیشرفته‌تر (۵+ سرویس با پایگاه‌های داده) می‌پردازید.

### فاز 2: گسترش آینده (معماری مرجع)

پس از تسلط بر معماری ۲ سرویسی، می‌توانید به موارد زیر گسترش دهید:

```
Full Architecture (Not Included - For Reference)
├── API Gateway (✅ Included)
├── Product Service (✅ Included)
├── Order Service (🔜 Add next)
├── User Service (🔜 Add next)
├── Notification Service (🔜 Add last)
├── Azure Service Bus (🔜 For async communication)
├── Cosmos DB (🔜 For product persistence)
├── Azure SQL (🔜 For order management)
└── Azure Storage (🔜 For file storage)
```

راهنمای "Expansion Guide" در انتها را برای دستورالعمل گام‌به‌گام ببینید.

## ویژگی‌های شامل‌شده

✅ **Service Discovery**: کشف خودکار مبتنی بر DNS بین کانتینرها  
✅ **Load Balancing**: تعادل بار داخلی بین نمونه‌ها  
✅ **Auto-scaling**: مقیاس‌بندی مستقل برای هر سرویس براساس درخواست‌های HTTP  
✅ **Health Monitoring**: بررسی‌های liveness و readiness برای هر دو سرویس  
✅ **Distributed Logging**: لاگ‌برداری متمرکز با Application Insights  
✅ **Internal Networking**: ارتباط امن سرویس به سرویس  
✅ **Container Orchestration**: استقرار و مقیاس‌دهی خودکار کانتینرها  
✅ **Zero-Downtime Updates**: به‌روزرسانی‌های Rolling با مدیریت بازبینی‌ها  

## پیش‌نیازها

### ابزارهای مورد نیاز

قبل از شروع، بررسی کنید که این ابزارها را نصب دارید:

1. **[ابزار خط فرمان توسعه‌دهنده Azure (azd)](https://learn.microsoft.com/azure/developer/azure-developer-cli/install-azd)** (نسخه 1.0.0 یا بالاتر)
   ```bash
   azd version
   # خروجی مورد انتظار: azd نسخه 1.0.0 یا بالاتر
   ```

2. **[Azure CLI](https://learn.microsoft.com/cli/azure/install-azure-cli)** (نسخه 2.50.0 یا بالاتر)
   ```bash
   az --version
   # خروجی مورد انتظار: azure-cli 2.50.0 یا بالاتر
   ```

3. **[Docker](https://www.docker.com/get-started)** (برای توسعه/آزمایش محلی - اختیاری)
   ```bash
   docker --version
   # خروجی مورد انتظار: نسخهٔ Docker ۲۰.۱۰ یا بالاتر
   ```

### نیازمندی‌های Azure

- یک **اشتراک Azure** فعال ([create a free account](https://azure.microsoft.com/free/))
- دسترسی برای ایجاد منابع در اشتراک شما
- نقش **Contributor** در اشتراک یا گروه منابع

### پیش‌نیازهای دانشی

این یک مثال سطح پیشرفته است. شما باید:
- مثال [Simple Flask API](../../../../../examples/container-app/simple-flask-api) را کامل کرده باشید
- درک پایه‌ای از معماری میکروسرویس‌ها داشته باشید
- با REST APIها و HTTP آشنایی داشته باشید
- با مفاهیم کانتینر آشنا باشید

**تازه‌کار با Container Apps؟** ابتدا با مثال [Simple Flask API](../../../../../examples/container-app/simple-flask-api) شروع کنید تا اصول را بیاموزید.

## شروع سریع (گام‌به‌گام)

### گام 1: کلون و جابه‌جایی

```bash
git clone https://github.com/microsoft/AZD-for-beginners.git
cd AZD-for-beginners/examples/container-app/microservices
```

**✓ بررسی موفقیت**: تأیید کنید `azure.yaml` را می‌بینید:
```bash
ls
# انتظار می‌رود: README.md, azure.yaml, infra/, src/
```

### گام 2: احراز هویت در Azure

```bash
azd auth login
```

این کار مرورگر شما را برای احراز هویت Azure باز می‌کند. با اطلاعات کاربری Azure خود وارد شوید.

**✓ بررسی موفقیت**: شما باید موارد زیر را ببینید:
```
Logged in to Azure.
```

### گام 3: مقداردهی اولیهٔ محیط

```bash
azd init
```

**پرسش‌هایی که خواهید دید**:
- **نام محیط**: یک نام کوتاه وارد کنید (مثلاً `microservices-dev`)
- **اشتراک Azure**: اشتراک خود را انتخاب کنید
- **منطقه Azure**: یک ناحیه انتخاب کنید (مثلاً `eastus`, `westeurope`)

**✓ بررسی موفقیت**: شما باید موارد زیر را ببینید:
```
SUCCESS: New project initialized!
```

### گام 4: استقرار زیرساخت و سرویس‌ها

```bash
azd up
```

**چه اتفاقی می‌افتد** (۸-۱۲ دقیقه طول می‌کشد):
1. ایجاد محیط Container Apps
2. ایجاد Application Insights برای مانیتورینگ
3. ساخت کانتینر API Gateway (Node.js)
4. ساخت کانتینر Product Service (Python)
5. استقرار هر دو کانتینر در Azure
6. پیکربندی شبکه و بررسی‌های سلامت
7. راه‌اندازی مانیتورینگ و لاگ‌برداری

**✓ بررسی موفقیت**: شما باید موارد زیر را ببینید:
```
SUCCESS: Your application was deployed to Azure in X minutes Y seconds.
Endpoint: https://api-gateway-<unique-id>.azurecontainerapps.io
```

**⏱️ زمان**: 8-12 دقیقه

### گام 5: آزمایش استقرار

```bash
# نقطهٔ پایانی دروازه را دریافت کنید
GATEWAY_URL=$(azd env get-values | grep API_GATEWAY_URL | cut -d '=' -f2 | tr -d '"')

# سلامت دروازهٔ API را آزمایش کنید
curl $GATEWAY_URL/health

# خروجی مورد انتظار:
# {"وضعیت":"سالم","سرویس":"دروازهٔ API","زمان":"2025-11-19T10:30:00Z"}
```

**سرویس محصول را از طریق گیت‌وی تست کنید**:
```bash
# فهرست محصولات
curl $GATEWAY_URL/api/products

# خروجی مورد انتظار:
# [
#   {"id":1,"name":"لپ‌تاپ","price":999.99,"stock":50},
#   {"id":2,"name":"ماوس","price":29.99,"stock":200},
#   {"id":3,"name":"صفحه‌کلید","price":79.99,"stock":150}
# ]
```

**✓ بررسی موفقیت**: هر دو endpoint داده JSON را بدون خطا برمی‌گردانند.

---

**🎉 تبریک!** شما یک معماری میکروسرویس را در Azure مستقر کردید!

## ساختار پروژه

تمام فایل‌های پیاده‌سازی شامل شده‌اند—این یک مثال کامل و عملی است:

```
microservices/
│
├── README.md                         # This file
├── azure.yaml                        # AZD configuration
├── .gitignore                        # Git ignore patterns
│
├── infra/                           # Infrastructure as Code (Bicep)
│   ├── main.bicep                   # Main orchestration
│   ├── abbreviations.json           # Naming conventions
│   ├── core/                        # Shared infrastructure
│   │   ├── container-apps-environment.bicep  # Container environment + registry
│   │   └── monitor.bicep            # Application Insights + Log Analytics
│   └── app/                         # Service definitions
│       ├── api-gateway.bicep        # API Gateway container app
│       └── product-service.bicep    # Product Service container app
│
└── src/                             # Application source code
    ├── api-gateway/                 # Node.js API Gateway
    │   ├── app.js                   # Express server with routing
    │   ├── package.json             # Node dependencies
    │   └── Dockerfile               # Container definition
    └── product-service/             # Python Product Service
        ├── main.py                  # Flask API with product data
        ├── requirements.txt         # Python dependencies
        └── Dockerfile               # Container definition
```

**هر جزء چه کاری انجام می‌دهد:**

**زیرساخت (infra/)**:
- `main.bicep`: هماهنگ‌کنندهٔ تمام منابع Azure و وابستگی‌های آنها
- `core/container-apps-environment.bicep`: ایجاد محیط Container Apps و Azure Container Registry
- `core/monitor.bicep`: راه‌اندازی Application Insights برای لاگ‌برداری توزیع‌شده
- `app/*.bicep`: تعاریف جداگانهٔ container appها با پیکربندی مقیاس و بررسی‌های سلامت

**API Gateway (src/api-gateway/)**:
- سرویس در معرضِ بیرون که درخواست‌ها را به سرویس‌های بک‌اند هدایت می‌کند
- لاگ‌برداری، مدیریت خطا و فوروارد کردن درخواست‌ها را پیاده‌سازی می‌کند
- نمونه‌ای از ارتباط HTTP سرویس به سرویس را نشان می‌دهد

**Product Service (src/product-service/)**:
- سرویس داخلی با کاتالوگ محصول (برای سادگی در حافظه نگهداری می‌شود)
- REST API با بررسی‌های سلامت
- نمونه‌ای از الگوی میکروسرویس بک‌اند

## نمای کلی سرویس‌ها

### API Gateway (Node.js/Express)

**پورت**: 8080  
**دسترسی**: عمومی (ingress خارجی)  
**هدف**: درخواست‌های ورودی را به سرویس‌های بک‌اند مربوطه هدایت می‌کند  

**Endpoints**:
- `GET /` - اطلاعات سرویس
- `GET /health` - endpoint بررسی سلامت
- `GET /api/products` - فوروارد به سرویس محصول (نمایش همه)
- `GET /api/products/:id` - فوروارد به سرویس محصول (دریافت بر اساس شناسه)

**ویژگی‌های کلیدی**:
- مسیر‌یابی درخواست با axios
- لاگ‌برداری متمرکز
- مدیریت خطا و زمان‌بندی (timeout)
- کشف سرویس از طریق متغیرهای محیطی
- یکپارچه‌سازی با Application Insights

**کد برجسته** (`src/api-gateway/app.js`):
```javascript
// ارتباط داخلی سرویس‌ها
app.get('/api/products', async (req, res) => {
  const response = await axios.get(`${PRODUCT_SERVICE_URL}/products`);
  res.json(response.data);
});
```

### Product Service (Python/Flask)

**پورت**: 8000  
**دسترسی**: فقط داخلی (بدون ingress خارجی)  
**هدف**: مدیریت کاتالوگ محصول با داده‌های در حافظه  

**Endpoints**:
- `GET /` - اطلاعات سرویس
- `GET /health` - endpoint بررسی سلامت
- `GET /products` - نمایش همه محصولات
- `GET /products/<id>` - دریافت محصول بر اساس شناسه

**ویژگی‌های کلیدی**:
- RESTful API با Flask
- ذخیره‌سازی محصولات در حافظه (ساده، بدون نیاز به پایگاه داده)
- مانیتورینگ سلامت با probes
- لاگ‌برداری ساخت‌یافته
- یکپارچه‌سازی با Application Insights

**مدل داده**:
```python
{
  "id": 1,
  "name": "Laptop",
  "description": "High-performance laptop",
  "price": 999.99,
  "stock": 50
}
```

**چرا فقط داخلی؟**
سرویس محصول به‌صورت عمومی در دسترس نیست. تمام درخواست‌ها باید از طریق API Gateway عبور کنند، که ویژگی‌های زیر را فراهم می‌کند:
- امنیت: نقطه دسترسی کنترل‌شده
- انعطاف‌پذیری: می‌تواند بک‌اند را بدون تأثیر بر مشتریان تغییر دهد
- مانیتورینگ: لاگ‌برداری درخواست‌ها به‌صورت متمرکز

## درک ارتباط سرویس‌ها

### چگونه سرویس‌ها با یکدیگر صحبت می‌کنند

در این مثال، API Gateway با استفاده از تماس‌های HTTP داخلی با Product Service ارتباط برقرار می‌کند:

```javascript
// درگاه API (src/api-gateway/app.js)
const PRODUCT_SERVICE_URL = process.env.PRODUCT_SERVICE_URL;

// ارسال درخواست HTTP داخلی
const response = await axios.get(`${PRODUCT_SERVICE_URL}/products`);
```

**نکات کلیدی**:

1. **کشف مبتنی بر DNS**: Container Apps به‌طور خودکار DNS برای سرویس‌های داخلی را فراهم می‌کند
   - FQDN سرویس محصول: `product-service.internal.<environment>.azurecontainerapps.io`
   - به‌صورت ساده‌شده: `http://product-service` (Container Apps آن را حل می‌کند)

2. **بدون افشای عمومی**: Product Service در Bicep دارای `external: false` است
   - فقط در داخل محیط Container Apps قابل دسترسی است
   - از اینترنت قابل دسترسی نیست

3. **متغیرهای محیطی**: آدرس سرویس‌ها هنگام استقرار تزریق می‌شوند
   - Bicep FQDN داخلی را به گیت‌وی پاس می‌دهد
   - هیچ URL ثابت‌شده‌ای در کد برنامه وجود ندارد

**تشبیه**: می‌توان این را شبیه اتاق‌های یک اداره دید. API Gateway میز پذیرش (در معرض عمومی) است، و Product Service اتاق اداری (فقط داخلی) است. بازدیدکنندگان باید از پذیرش عبور کنند تا به هر اتاقی برسند.

## گزینه‌های استقرار

### استقرار کامل (پیشنهادی)

```bash
# زیرساخت و هر دو سرویس را مستقر کنید
azd up
```

این موارد را مستقر می‌کند:
1. محیط Container Apps
2. Application Insights
3. Container Registry
4. کانتینر API Gateway
5. کانتینر Product Service

**زمان**: 8-12 دقیقه

### استقرار یک سرویس جداگانه

```bash
# تنها یک سرویس را مستقر کنید (بعد از اجرای اولیه azd up)
azd deploy api-gateway

# یا سرویس product را مستقر کنید
azd deploy product-service
```

**مورد استفاده**: زمانی که کد یکی از سرویس‌ها را به‌روزرسانی کرده‌اید و می‌خواهید فقط آن سرویس را مجدداً مستقر کنید.

### به‌روزرسانی پیکربندی

```bash
# پارامترهای مقیاس‌بندی را تغییر دهید
azd env set GATEWAY_MAX_REPLICAS 30

# با پیکربندی جدید مجدداً مستقر کنید
azd up
```

## پیکربندی

### پیکربندی مقیاس‌پذیری

هر دو سرویس در فایل‌های Bicep خود با مقیاس خودکار مبتنی بر HTTP پیکربندی شده‌اند:

**API Gateway**:
- حداقل نسخه‌ها: 2 (همیشه حداقل 2 برای در دسترس بودن)
- حداکثر نسخه‌ها: 20
- ماشه مقیاس: 50 درخواست هم‌زمان به ازای هر نسخه

**Product Service**:
- حداقل نسخه‌ها: 1 (در صورت نیاز می‌تواند تا صفر مقیاس شود)
- حداکثر نسخه‌ها: 10
- ماشه مقیاس: 100 درخواست هم‌زمان به ازای هر نسخه

**سفارشی‌سازی مقیاس** (در `infra/app/*.bicep`):
```bicep
scale: {
  minReplicas: 1
  maxReplicas: 10
  rules: [
    {
      name: 'http-scale-rule'
      http: {
        metadata: {
          concurrentRequests: '100'  // Adjust this
        }
      }
    }
  ]
}
```

### تخصیص منابع

**API Gateway**:
- CPU: 1.0 vCPU
- حافظه: 2 GiB
- دلیل: هندل کردن تمام ترافیک خارجی

**Product Service**:
- CPU: 0.5 vCPU
- حافظه: 1 GiB
- دلیل: عملیات سبک در حافظه

### بررسی‌های سلامت

هر دو سرویس شامل probes مربوط به liveness و readiness هستند:

```bicep
probes: [
  {
    type: 'Liveness'
    httpGet: {
      path: '/health'
      port: 8080
    }
    initialDelaySeconds: 10
    periodSeconds: 30
  }
  {
    type: 'Readiness'
    httpGet: {
      path: '/health'
      port: 8080
    }
    initialDelaySeconds: 5
    periodSeconds: 10
  }
]
```

**معنی این موضوع**:
- **Liveness**: اگر بررسی سلامت شکست بخورد، Container Apps کانتینر را ری‌استارت می‌کند
- **Readiness**: اگر آماده نباشد، Container Apps ترافیک را به آن نسخه هدایت نمی‌کند



## مانیتورینگ و مشاهده‌پذیری

### مشاهده لاگ‌های سرویس

```bash
# مشاهدهٔ لاگ‌ها با azd monitor
azd monitor --logs

# یا از Azure CLI برای Container Apps خاص استفاده کنید:
# پخش زندهٔ لاگ‌ها از API Gateway
az containerapp logs show --name api-gateway --resource-group $RG_NAME --follow

# مشاهدهٔ لاگ‌های اخیر سرویس محصول
az containerapp logs show --name product-service --resource-group $RG_NAME --tail 100
```

**خروجی مورد انتظار**:
```
[api-gateway] API Gateway listening on port 8080
[api-gateway] Product Service URL: http://product-service
[api-gateway] GET /api/products 200 - 45ms
[product-service] Retrieved 5 products
```

### پرس‌وجوهای Application Insights

به Application Insights در Azure Portal دسترسی پیدا کنید، سپس این پرس‌وجوها را اجرا کنید:

**یافتن درخواست‌های کند**:
```kusto
requests
| where timestamp > ago(1h)
| where duration > 1000  // Requests taking >1 second
| summarize count() by name, cloud_RoleName
| order by count_ desc
```

**ردیابی تماس‌های سرویس به سرویس**:
```kusto
dependencies
| where timestamp > ago(1h)
| where type == "Http"
| project timestamp, name, target, duration, success
| order by timestamp desc
```

**نرخ خطا بر اساس سرویس**:
```kusto
exceptions
| where timestamp > ago(24h)
| summarize errorCount = count() by cloud_RoleName, type
| order by errorCount desc
```

**حجم درخواست در طول زمان**:
```kusto
requests
| where timestamp > ago(1h)
| summarize requestCount = count() by bin(timestamp, 5m), cloud_RoleName
| render timechart
```

### دسترسی به داشبورد مانیتورینگ

```bash
# جزئیات Application Insights را دریافت کنید
azd env get-values | grep APPLICATIONINSIGHTS

# بخش مانیتورینگ پورتال Azure را باز کنید
az monitor app-insights component show \
  --app $(azd env get-values | grep APPLICATIONINSIGHTS_CONNECTION_STRING | cut -d '=' -f2) \
  --resource-group $(azd env get-values | grep AZURE_RESOURCE_GROUP | cut -d '=' -f2) \
  --query "appId" -o tsv
```

### متریک‌های زنده

1. به Application Insights در Azure Portal بروید
2. روی "Live Metrics" کلیک کنید
3. درخواست‌ها، خطاها و عملکرد را به‌صورت آنی ببینید
4. تست با اجرای: `curl $(azd env get-values | grep API_GATEWAY_URL | cut -d '=' -f2 | tr -d '"')/api/products`

## تمرین‌های عملی

[توجه: تمرین‌های کامل را در بخش "Practical Exercises" بالا ببینید که شامل تمرین‌های گام‌به‌گام برای تأیید استقرار، تغییر داده‌ها، آزمایش مقیاس خودکار، مدیریت خطا و افزودن سرویس سوم است.]

## تحلیل هزینه

### هزینه‌های تخمینی ماهانه (برای این مثال ۲ سرویسی)

| Resource | Configuration | Estimated Cost |
|----------|--------------|----------------|
| API Gateway | 2-20 replicas, 1 vCPU, 2GB RAM | $30-150 |
| Product Service | 1-10 replicas, 0.5 vCPU, 1GB RAM | $15-75 |
| Container Registry | Basic tier | $5 |
| Application Insights | 1-2 GB/month | $5-10 |
| Log Analytics | 1 GB/month | $3 |
| **کل** | | **$58-243/month** |

**شکست هزینه بر اساس استفاده**:
- **ترافیک کم** (آزمایش/یادگیری): ~$60/month
- **ترافیک متوسط** (پروداکشن کوچک): ~$120/month
- **ترافیک بالا** (دوره‌های پرترافیک): ~$240/month

### نکات بهینه‌سازی هزینه

1. **مقیاس تا صفر برای توسعه**:
   ```bicep
   scale: {
     minReplicas: 0  // Save $30-40/month when not in use
     maxReplicas: 10
   }
   ```

2. **استفاده از Consumption Plan برای Cosmos DB** (وقتی اضافه می‌کنید):
   - تنها برای آنچه استفاده می‌کنید پرداخت کنید
   - بدون هزینه حداقلی

3. **تنظیم نمونه‌برداری در Application Insights**:
   ```javascript
   appInsights.defaultClient.config.samplingPercentage = 50; // نمونه‌برداری از ۵۰٪ درخواست‌ها
   ```

4. **پاک‌سازی هنگام عدم استفاده**:
   ```bash
   azd down
   ```

### گزینه‌های سطح رایگان
برای یادگیری/آزمایش، در نظر بگیرید:
- از اعتبار رایگان Azure استفاده کنید (اولین 30 روز)
- تعداد replicas را در حداقل نگه دارید
- پس از آزمایش حذف کنید (بدون هزینه‌های جاری)

---

## پاکسازی

برای جلوگیری از هزینه‌های جاری، همه‌ی منابع را حذف کنید:

```bash
azd down --force --purge
```

**پیام تأیید**:
```
? Total resources to delete: 6, are you sure you want to continue? (y/N)
```

برای تأیید `y` را تایپ کنید.

**چه چیزهایی حذف می‌شوند**:
- Container Apps Environment
- هر دو Container Apps (gateway & product service)
- Container Registry
- Application Insights
- Log Analytics Workspace
- Resource Group

**✓ تأیید پاکسازی**:
```bash
az group list --query "[?starts_with(name,'rg-microservices')]" --output table
```

باید خالی برگردد.

---

## راهنمای گسترش: از 2 تا 5+ سرویس

وقتی معماری 2 سرویسه را یاد گرفتید، روش گسترش به این صورت است:

### فاز 1: افزودن پایداری پایگاه‌داده (گام بعدی)

**افزودن Cosmos DB برای سرویس محصول**:

1. فایل `infra/core/cosmos.bicep` را ایجاد کنید:
   ```bicep
   resource cosmosAccount 'Microsoft.DocumentDB/databaseAccounts@2023-04-15' = {
     name: name
     location: location
     kind: 'GlobalDocumentDB'
     properties: {
       databaseAccountOfferType: 'Standard'
       locations: [{ locationName: location, failoverPriority: 0 }]
     }
   }
   ```

2. سرویس محصول را به‌روزرسانی کنید تا به‌جای داده‌های درون‌حافظه‌ای از Cosmos DB استفاده کند

3. هزینه اضافی تقریبی: ~$25/ماه (serverless)

### فاز 2: افزودن سرویس سوم (مدیریت سفارش‌ها)

**ایجاد سرویس سفارش (Order Service)**:

1. پوشه جدید: `src/order-service/` (Python/Node.js/C#)
2. Bicep جدید: `infra/app/order-service.bicep`
3. API Gateway را به‌روزرسانی کنید تا مسیر `/api/orders` را هدایت کند
4. افزودن Azure SQL Database برای پایداری سفارش‌ها

**معماری به این صورت می‌شود**:
```
API Gateway → Product Service (Cosmos DB)
           → Order Service (Azure SQL)
```

### فاز 3: افزودن ارتباط غیرهمزمان (Service Bus)

**پیاده‌سازی معماری رویداد‌محور**:

1. افزودن Azure Service Bus: `infra/core/servicebus.bicep`
2. سرویس محصول رویدادهای "ProductCreated" را منتشر می‌کند
3. Order Service به رویدادهای محصول مشترک می‌شود
4. افزودن Notification Service برای پردازش رویدادها

**الگو**: Request/Response (HTTP) + Event-Driven (Service Bus)

### فاز 4: افزودن احراز هویت کاربر

**پیاده‌سازی سرویس کاربر**:

1. ایجاد `src/user-service/` (Go/Node.js)
2. افزودن Azure AD B2C یا احراز هویت JWT سفارشی
3. API Gateway توکن‌ها را اعتبارسنجی می‌کند
4. سرویس‌ها مجوزهای کاربر را بررسی می‌کنند

### فاز 5: آماده‌سازی برای تولید

**افزودن این اجزا**:
- Azure Front Door (توزیع بار سراسری)
- Azure Key Vault (مدیریت اسرار)
- Azure Monitor Workbooks (داشبوردهای سفارشی)
- خط لوله CI/CD (GitHub Actions)
- استقرار Blue-Green
- Managed Identity برای تمام سرویس‌ها

**هزینه کلی معماری تولید**: ~$300-1,400/ماه

---

## اطلاعات بیشتر

### مستندات مرتبط
- [Azure Container Apps Documentation](https://learn.microsoft.com/azure/container-apps/)
- [Microservices Architecture Guide](https://learn.microsoft.com/azure/architecture/guide/architecture-styles/microservices)
- [Application Insights for Distributed Tracing](https://learn.microsoft.com/azure/azure-monitor/app/distributed-tracing)
- [Azure Developer CLI Documentation](https://learn.microsoft.com/azure/developer/azure-developer-cli/)

### گام‌های بعدی در این دوره
- ← قبلی: [Simple Flask API](../../../../../examples/container-app/simple-flask-api) - مثال مبتدی تک‌کانتینری
- → بعدی: [AI Integration Guide](../../../../../examples/docs/ai-foundry) - افزودن قابلیت‌های AI
- 🏠 [خانه دوره](../../README.md)

### مقایسه: چه زمانی از چه چیزی استفاده کنیم

**Single Container App** (مثال ساده Flask API):
- ✅ برنامه‌های ساده
- ✅ معماری مونولیتیک
- ✅ سریع برای استقرار
- ❌ قابلیت مقیاس‌پذیری محدود
- **هزینه**: ~$15-50/ماه

**Microservices** (این مثال):
- ✅ برنامه‌های پیچیده
- ✅ مقیاس‌پذیری مستقل برای هر سرویس
- ✅ استقلال تیم‌ها (سرویس‌های مختلف، تیم‌های مختلف)
- ❌ پیچیدگی مدیریتی بیشتر
- **هزینه**: ~$60-250/ماه

**Kubernetes (AKS)**:
- ✅ کنترل و انعطاف‌پذیری حداکثری
- ✅ قابل حمل بین چند ابر
- ✅ شبکه‌بندی پیشرفته
- ❌ نیازمند مهارت در Kubernetes
- **هزینه**: ~$150-500/ماه حداقل

**توصیه**: با Container Apps (این مثال) شروع کنید، تنها در صورت نیاز به قابلیت‌های خاص Kubernetes به AKS مهاجرت کنید.

---

## پرسش‌های متداول

**Q: چرا فقط 2 سرویس به جای 5+؟**  
A: پیشرفت آموزشی. اصول پایه (ارتباط سرویس‌ها، مانیتورینگ، مقیاس‌بندی) را با یک مثال ساده یاد بگیرید قبل از افزودن پیچیدگی. الگوهایی که اینجا می‌آموزید در معماری‌های 100 سرویسی نیز کاربرد دارند.

**Q: آیا خودم می‌توانم سرویس‌های بیشتری اضافه کنم؟**  
A: قطعاً! راهنمای گسترش بالا را دنبال کنید. هر سرویس جدید از همان الگو پیروی می‌کند: ایجاد پوشه src، ایجاد فایل Bicep، به‌روزرسانی azure.yaml، و استقرار.

**Q: آیا این آماده تولید است؟**  
A: این یک پایه محکم است. برای تولید، موارد زیر را اضافه کنید: Managed Identity، Key Vault، پایگاه‌داده‌های پایدار، خط لوله CI/CD، هشدارهای مانیتورینگ، و استراتژی پشتیبان‌گیری.

**Q: چرا از Dapr یا سایر service meshها استفاده نکنیم؟**  
A: برای یادگیری ساده نگهش دارید. وقتی شبکه‌بندی بومی Container Apps را درک کردید، می‌توانید Dapr را برای سناریوهای پیشرفته اضافه کنید.

**Q: چگونه به‌صورت محلی اشکال‌زدایی کنم؟**  
A: سرویس‌ها را به‌صورت محلی با Docker اجرا کنید:
```bash
cd src/api-gateway
docker build -t local-gateway .
docker run -p 8080:8080 -e PRODUCT_SERVICE_URL=http://localhost:8000 local-gateway
```

**Q: آیا می‌توانم از زبان‌های برنامه‌نویسی مختلف استفاده کنم؟**  
A: بله! این مثال نشان‌دهنده Node.js (gateway) + Python (product service) است. می‌توانید هر زبانی را که در کانتینر اجرا می‌شود ترکیب کنید.

**Q: اگر اعتبار Azure نداشته باشم چه؟**  
A: از سطح رایگان Azure استفاده کنید (۳۰ روز اول برای حساب‌های جدید) یا برای دوره‌های آزمایشی کوتاه مستقر کنید و بلافاصله حذف کنید.

---

> **🎓 خلاصه مسیر یادگیری**: شما یاد گرفته‌اید چگونه یک معماری چندسرویسی را با مقیاس‌دهی خودکار، شبکه‌بندی داخلی، مانیتورینگ متمرکز و الگوهای آماده تولید مستقر کنید. این پایه شما را برای سیستم‌های توزیع‌شده پیچیده و معماری‌های میکروسرویسی سازمانی آماده می‌کند.

**📚 ناوبری دوره:**
- ← قبلی: [Simple Flask API](../../../../../examples/container-app/simple-flask-api)
- → بعدی: [Database Integration Example](../../../../../examples/database-app)
- 🏠 [خانه دوره](../../../README.md)
- 📖 [بهترین شیوه‌های Container Apps](../../../docs/chapter-04-infrastructure/deployment-guide.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
سلب مسئولیت:
این سند با استفاده از سرویس ترجمهٔ هوش مصنوعی [Co-op Translator](https://github.com/Azure/co-op-translator) ترجمه شده است. در حالی که ما برای دقت تلاش می‌کنیم، لطفاً توجه داشته باشید که ترجمه‌های خودکار ممکن است حاوی خطاها یا نادرستی‌هایی باشند. نسخهٔ اصلی سند به زبان مبدأ باید به عنوان منبع معتبر در نظر گرفته شود. برای اطلاعات حیاتی، ترجمهٔ حرفه‌ای انسانی توصیه می‌شود. ما در قبال هرگونه سوءتفاهم یا تفسیر نادرستی که از استفاده از این ترجمه ناشی شود مسئولیتی نداریم.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->