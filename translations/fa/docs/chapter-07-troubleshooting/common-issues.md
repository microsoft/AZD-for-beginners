# مسائل و راه‌حل‌های متداول

**ناوبری فصل:**
- **📚 صفحه دوره**: [AZD برای مبتدیان](../../README.md)
- **📖 فصل فعلی**: فصل 7 - عیب‌یابی و رفع اشکال
- **⬅️ فصل قبلی**: [فصل 6: بررسی‌های پیش از استقرار](../chapter-06-pre-deployment/preflight-checks.md)
- **➡️ بعدی**: [راهنمای رفع اشکال](debugging.md)
- **🚀 فصل بعدی**: [فصل 8: الگوهای تولید و سازمانی](../chapter-08-production/production-ai-practices.md)

## مقدمه

این راهنمای جامع عیب‌یابی پوشش‌دهنده رایج‌ترین مشکلاتی است که در هنگام استفاده از Azure Developer CLI با آن‌ها مواجه می‌شوید. بیاموزید چگونه مشکلات مربوط به احراز هویت، استقرار، فراهم‌سازی زیرساخت و پیکربندی برنامه را تشخیص داده، عیب‌یابی و رفع کنید. هر مسئله شامل نشانه‌ها، دلایل ریشه‌ای و مراحل گام‌به‌گام حل مشکل است.

## اهداف یادگیری

با تکمیل این راهنما، شما قادر خواهید بود:
- تسلط بر تکنیک‌های تشخیصی برای مشکلات Azure Developer CLI
- درک مسائل رایج احراز هویت و مجوزها و راه‌حل‌های آن‌ها
- رفع خطاهای شکست استقرار، خطاهای فراهم‌سازی زیرساخت و مشکلات پیکربندی
- پیاده‌سازی استراتژی‌های پیشگیرانه نظارت و اشکال‌زدایی
- اعمال روش‌شناسی‌های سیستماتیک عیب‌یابی برای مسائل پیچیده
- پیکربندی لاگ‌گیری و مانیتورینگ مناسب برای جلوگیری از مشکلات آتی

## نتایج یادگیری

پس از اتمام، شما قادر خواهید بود:
- تشخیص مشکلات Azure Developer CLI با استفاده از ابزارهای تشخیصی داخلی
- رفع مستقل مشکلات مربوط به احراز هویت، اشتراک و مجوزها
- عیب‌یابی موثر شکست‌های استقرار و خطاهای فراهم‌سازی زیرساخت
- اشکال‌زدایی مشکلات پیکربندی برنامه و مسائل مخصوص محیط
- پیاده‌سازی مانیتورینگ و هشداردهی برای شناسایی پیشگیرانه مشکلات احتمالی
- اعمال بهترین شیوه‌ها برای لاگ‌گیری، اشکال‌زدایی و گردش کار حل مشکل

## تشخیص سریع

قبل از پرداختن به مسائل خاص، این دستورات را اجرا کنید تا اطلاعات تشخیصی جمع‌آوری شود:

```bash
# نسخه و وضعیت سلامت azd را بررسی کنید
azd version
azd config list

# احراز هویت Azure را بررسی کنید
az account show
az account list

# محیط فعلی را بررسی کنید
azd env show
azd env get-values

# لاگ‌گیری اشکال‌زدایی را فعال کنید
export AZD_DEBUG=true
azd <command> --debug
```

## مشکلات احراز هویت

### مسئله: "Failed to get access token"
**نشانه‌ها:**
- `azd up` با خطاهای احراز هویت مواجه می‌شود
- دستورات پیام «unauthorized» یا «access denied» را بازمی‌گردانند

**راه‌حل‌ها:**
```bash
# 1. احراز هویت مجدد با Azure CLI
az login
az account show

# 2. پاک‌سازی اعتبارنامه‌های کش‌شده
az account clear
az login

# 3. استفاده از جریان کد دستگاه (برای سیستم‌های بدون رابط گرافیکی)
az login --use-device-code

# 4. تنظیم اشتراک به‌صورت صریح
az account set --subscription "your-subscription-id"
azd config set defaults.subscription "your-subscription-id"
```

### مسئله: "Insufficient privileges" هنگام استقرار
**نشانه‌ها:**
- استقرار با خطاهای مجوز شکست می‌خورد
- نمی‌توان برخی منابع Azure را ایجاد کرد

**راه‌حل‌ها:**
```bash
# 1. تخصیص‌های نقش Azure خود را بررسی کنید
az role assignment list --assignee $(az account show --query user.name -o tsv)

# 2. اطمینان حاصل کنید که نقش‌های مورد نیاز را دارید
# - Contributor (برای ایجاد منابع)
# - User Access Administrator (برای اختصاص نقش‌ها)

# 3. برای دریافت مجوزهای مناسب با مدیر Azure خود تماس بگیرید
```

### مسئله: مشکلات احراز هویت چندمستاجری
**راه‌حل‌ها:**
```bash
# 1. ورود با مستاجر مشخص
az login --tenant "your-tenant-id"

# 2. تنظیم مستاجر در پیکربندی
azd config set auth.tenantId "your-tenant-id"

# 3. در صورت تغییر مستاجر، کش مستاجر را پاک کنید
az account clear
```

## 🏗️ خطاهای فراهم‌سازی زیرساخت

### مسئله: تداخل نام منابع
**نشانه‌ها:**
- خطاهای «The resource name already exists»
- استقرار در هنگام ایجاد منابع شکست می‌خورد

**راه‌حل‌ها:**
```bash
# 1. از نام‌های یکتای منابع با توکن‌ها استفاده کنید
# در قالب Bicep خود:
var resourceToken = toLower(uniqueString(subscription().id, environmentName, location))
name: '${applicationName}-${resourceToken}'

# 2. نام محیط را تغییر دهید
azd env new my-app-dev-$(whoami)-$(date +%s)

# 3. منابع موجود را پاک‌سازی کنید
azd down --force --purge
```

### مسئله: مکان/منطقه در دسترس نیست
**نشانه‌ها:**
- «The location 'xyz' is not available for resource type»
- برخی SKUها در منطقه انتخاب‌شده موجود نیستند

**راه‌حل‌ها:**
```bash
# ۱. مکان‌های در دسترس برای انواع منابع را بررسی کنید
az provider show --namespace Microsoft.Web --query "resourceTypes[?resourceType=='sites'].locations" -o table

# ۲. از مناطق معمولاً در دسترس استفاده کنید
azd config set defaults.location eastus2
# یا
azd env set AZURE_LOCATION eastus2

# ۳. در دسترس بودن سرویس را بر اساس منطقه بررسی کنید
# به آدرس زیر مراجعه کنید: https://azure.microsoft.com/global-infrastructure/services/
```

### مسئله: خطاهای تجاوز از سهمیه
**نشانه‌ها:**
- «Quota exceeded for resource type»
- «Maximum number of resources reached»

**راه‌حل‌ها:**
```bash
# 1. بررسی میزان استفاده فعلی از سهمیه
az vm list-usage --location eastus2 -o table

# 2. درخواست افزایش سهمیه از طریق پرتال Azure
# به مسیر زیر بروید: اشتراک‌ها > استفاده + سهمیه‌ها

# 3. از SKUهای کوچکتر برای توسعه استفاده کنید
# در فایل main.parameters.json:
{
  "appServiceSku": {
    "value": "B1"  // Instead of P1v3
  }
}

# 4. پاک‌سازی منابع بلااستفاده
az resource list --query "[?contains(name, 'unused')]" -o table
```

### مسئله: خطاهای قالب Bicep
**نشانه‌ها:**
- شکست‌های اعتبارسنجی قالب
- خطاهای سینتکس در فایل‌های Bicep

**راه‌حل‌ها:**
```bash
# 1. اعتبارسنجی نحو Bicep
az bicep build --file infra/main.bicep

# 2. از لینتر Bicep استفاده کنید
az bicep lint --file infra/main.bicep

# 3. بررسی نحو فایل پارامتر
cat infra/main.parameters.json | jq '.'

# 4. پیش‌نمایش تغییرات استقرار
azd provision --preview
```

## 🚀 شکست‌های استقرار

### مسئله: شکست‌های ساخت
**نشانه‌ها:**
- برنامه در طول استقرار موفق به ساخت نمی‌شود
- خطاهای نصب بسته‌ها

**راه‌حل‌ها:**
```bash
# 1. خروجی ساخت را با فلگ اشکال‌زدایی بررسی کنید
azd deploy --service web --debug

# 2. وضعیت سرویس مستقر شده را مشاهده کنید
azd show

# 3. ساخت را به صورت محلی تست کنید
cd src/web
npm install
npm run build

# 3. سازگاری نسخه‌های Node.js و Python را بررسی کنید
node --version  # باید با تنظیمات azure.yaml مطابقت داشته باشد
python --version

# 4. کش ساخت را پاک کنید
rm -rf node_modules package-lock.json
npm install

# 5. در صورت استفاده از کانتینرها، Dockerfile را بررسی کنید
docker build -t test-image .
docker run --rm test-image
```

### مسئله: شکست‌های استقرار کانتینر
**نشانه‌ها:**
- برنامه‌های کانتینری راه‌اندازی نمی‌شوند
- خطاهای کشیدن ایمیج

**راه‌حل‌ها:**
```bash
# 1. ساخت Docker را به‌صورت محلی تست کنید
docker build -t my-app:latest .
docker run --rm -p 3000:3000 my-app:latest

# 2. لاگ‌های کانتینر را با استفاده از Azure CLI بررسی کنید
az containerapp logs show --name my-app --resource-group my-rg --follow

# 3. برنامه را از طریق azd مانیتور کنید
azd monitor --logs

# 3. دسترسی به رجیستری کانتینر را تأیید کنید
az acr login --name myregistry

# 4. پیکربندی برنامه کانتینر را بررسی کنید
az containerapp show --name my-app --resource-group my-rg
```

### مسئله: اختلال در اتصال به پایگاه‌داده
**نشانه‌ها:**
- برنامه قادر به اتصال به پایگاه‌داده نیست
- خطاهای تایم‌اوت اتصال

**راه‌حل‌ها:**
```bash
# 1. قوانین فایروال پایگاه داده را بررسی کنید
az postgres flexible-server firewall-rule list --name mydb --resource-group myrg

# 2. اتصال از برنامه را آزمایش کنید
# آن را موقتاً به برنامه‌تان اضافه کنید:
curl -v telnet://mydb.postgres.database.azure.com:5432

# 3. فرمت رشتهٔ اتصال را تأیید کنید
azd env get-values | grep DATABASE

# 4. وضعیت سرور پایگاه داده را بررسی کنید
az postgres flexible-server show --name mydb --resource-group myrg --query state
```

## 🔧 مسائل پیکربندی

### مسئله: متغیرهای محیطی کار نمی‌کنند
**نشانه‌ها:**
- برنامه نمی‌تواند مقادیر پیکربندی را بخواند
- متغیرهای محیطی خالی به نظر می‌رسند

**راه‌حل‌ها:**
```bash
# 1. اطمینان حاصل کنید که متغیرهای محیطی تنظیم شده‌اند
azd env get-values
azd env get DATABASE_URL

# 2. نام متغیرها را در azure.yaml بررسی کنید
cat azure.yaml | grep -A 5 env:

# 3. برنامه را مجدداً راه‌اندازی کنید
azd deploy --service web

# 4. پیکربندی App Service را بررسی کنید
az webapp config appsettings list --name myapp --resource-group myrg
```

### مسئله: مشکلات گواهی SSL/TLS
**نشانه‌ها:**
- HTTPS کار نمی‌کند
- خطاهای اعتبارسنجی گواهی

**راه‌حل‌ها:**
```bash
# ۱. وضعیت گواهی SSL را بررسی کنید
az webapp config ssl list --resource-group myrg

# ۲. فقط HTTPS را فعال کنید
az webapp update --name myapp --resource-group myrg --https-only true

# ۳. دامنهٔ سفارشی را اضافه کنید (در صورت نیاز)
az webapp config hostname add --webapp-name myapp --resource-group myrg --hostname mydomain.com
```

### مسئله: مشکلات پیکربندی CORS
**نشانه‌ها:**
- فرانت‌اند نمی‌تواند API را فراخوانی کند
- درخواست‌های تغییر مبدأ مسدود می‌شوند

**راه‌حل‌ها:**
```bash
# 1. پیکربندی CORS برای App Service
az webapp cors add --name myapi --resource-group myrg --allowed-origins https://myapp.azurewebsites.net

# 2. به‌روزرسانی API برای رسیدگی به CORS
# در Express.js:
app.use(cors({
  origin: process.env.FRONTEND_URL,
  credentials: true
}));

# 3. بررسی اینکه روی URLهای صحیح اجرا می‌شود
azd show
```

## 🌍 مسائل مدیریت محیط

### مسئله: مشکلات سوئیچ محیط
**نشانه‌ها:**
- محیط اشتباه استفاده می‌شود
- پیکربندی به درستی تغییر نمی‌کند

**راه‌حل‌ها:**
```bash
# ۱. فهرست تمام محیط‌ها
azd env list

# ۲. به‌صورت صریح محیط را انتخاب کنید
azd env select production

# ۳. بررسی محیط فعلی
azd env show

# ۴. در صورت خراب شدن، محیط جدید ایجاد کنید
azd env new production-new
azd env select production-new
```

### مسئله: خراب شدن محیط
**نشانه‌ها:**
- محیط وضعیت نامعتبری نشان می‌دهد
- منابع با پیکربندی همخوانی ندارند

**راه‌حل‌ها:**
```bash
# 1. به‌روزرسانی وضعیت محیط
azd env refresh

# 2. بازنشانی پیکربندی محیط
azd env new production-reset
# متغیرهای محیطی مورد نیاز را کپی کنید
azd env set DATABASE_URL "your-value"

# 3. وارد کردن منابع موجود (در صورت امکان)
# فایل .azure/production/config.json را به‌صورت دستی با شناسه‌های منابع به‌روز کنید
```

## 🔍 مسائل عملکرد

### مسئله: زمان‌های طولانی استقرار
**نشانه‌ها:**
- استقرارها زمان زیادی می‌گیرند
- تایم‌اوت در طول استقرار رخ می‌دهد

**راه‌حل‌ها:**
```bash
# 1. برای تکرار سریع‌تر، سرویس‌های مشخص را مستقر کنید
azd deploy --service web
azd deploy --service api

# 2. زمانی که زیرساخت تغییر نکرده است، از استقرار صرفاً مبتنی بر کد استفاده کنید
azd deploy  # سریع‌تر از azd up

# 3. فرآیند ساخت را بهینه کنید
# در package.json:
"scripts": {
  "build": "webpack --mode=production --optimize-minimize"
}

# 4. محل منابع را بررسی کنید (از همان منطقه استفاده کنید)
azd config set defaults.location eastus2
```

### مسئله: مشکلات عملکرد برنامه
**نشانه‌ها:**
- زمان پاسخ‌دهی کند
- استفاده بالا از منابع

**راه‌حل‌ها:**
```bash
# 1. افزایش منابع
# SKU را در main.parameters.json به‌روزرسانی کنید:
"appServiceSku": {
  "value": "S2"  // Scale up from B1
}

# 2. فعال‌سازی مانیتورینگ Application Insights
azd monitor --overview

# 3. بررسی لاگ‌های برنامه در Azure
az webapp log tail --name myapp --resource-group myrg
# یا برای Container Apps:
az containerapp logs show --name myapp --resource-group myrg --follow

# 4. پیاده‌سازی کش
# کش Redis را به زیرساخت خود اضافه کنید
```

## 🛠️ ابزارها و دستورات عیب‌یابی

### دستورات اشکال‌زدایی
```bash
# اشکال‌زدایی جامع
export AZD_DEBUG=true
azd up --debug 2>&1 | tee debug.log

# نسخه azd را بررسی کنید
azd version

# پیکربندی فعلی را مشاهده کنید
azd config list

# اتصال را آزمایش کنید
curl -v https://myapp.azurewebsites.net/health
```

### تحلیل لاگ
```bash
# گزارش‌های برنامه از طریق Azure CLI
az webapp log tail --name myapp --resource-group myrg

# نظارت بر برنامه با azd
azd monitor --logs
azd monitor --live

# گزارش‌های منابع Azure
az monitor activity-log list --resource-group myrg --start-time 2024-01-01 --max-events 50

# گزارش‌های کانتینر (برای Container Apps)
az containerapp logs show --name myapp --resource-group myrg --follow
```

### بررسی منابع
```bash
# فهرست تمام منابع
az resource list --resource-group myrg -o table

# بررسی وضعیت منبع
az webapp show --name myapp --resource-group myrg --query state

# عیب‌یابی شبکه
az network watcher test-connectivity --source-resource myvm --dest-address myapp.azurewebsites.net --dest-port 443
```

## 🆘 دریافت کمک اضافی

### چه زمانی باید ارجاع دهید
- مشکلات احراز هویت پس از امتحان تمام راه‌حل‌ها ادامه دارد
- مشکلات زیرساختی با سرویس‌های Azure
- مسائل مربوط به صورتحساب یا اشتراک
- نگرانی‌ها یا حوادث امنیتی

### کانال‌های پشتیبانی
```bash
# 1. بررسی وضعیت سرویس Azure Service Health
az rest --method get --uri "https://management.azure.com/subscriptions/{subscription-id}/providers/Microsoft.ResourceHealth/availabilityStatuses?api-version=2020-05-01"

# 2. ایجاد تیکت پشتیبانی Azure
# برو به: https://portal.azure.com -> راهنما و پشتیبانی

# 3. منابع انجمن
# - Stack Overflow: برچسب azure-developer-cli
# - گزارش‌های GitHub: https://github.com/Azure/azure-dev/issues
# - پرسش و پاسخ مایکروسافت: https://learn.microsoft.com/en-us/answers/
```

### اطلاعاتی که باید جمع‌آوری کنید
قبل از تماس با پشتیبانی، جمع‌آوری کنید:
- خروجی `azd version`
- خروجی `azd config list`
- خروجی `azd show` (وضعیت فعلی استقرار)
- پیام‌های خطا (متن کامل)
- مراحل بازتولید مشکل
- جزئیات محیط (`azd env show`)
- جدول زمانی شروع مشکل

### اسکریپت جمع‌آوری لاگ
```bash
#!/bin/باش
# جمع‌آوری-اطلاعات-عیب‌یابی.sh

echo "Collecting azd debug information..."
mkdir -p debug-logs

echo "System Information:" > debug-logs/system-info.txt
azd version >> debug-logs/system-info.txt
az --version >> debug-logs/system-info.txt

echo "Configuration:" > debug-logs/config.txt
azd config list >> debug-logs/config.txt
azd env show >> debug-logs/config.txt
azd env get-values >> debug-logs/config.txt

echo "Current deployment status:" > debug-logs/status.txt
azd show >> debug-logs/status.txt

echo "Debug information collected in debug-logs/"
```

## 📊 پیشگیری از مسائل

### چک‌لیست پیش از استقرار
```bash
# 1. اعتبارسنجی احراز هویت
az account show

# 2. بررسی سهمیه‌ها و محدودیت‌ها
az vm list-usage --location eastus2

# 3. اعتبارسنجی قالب‌ها
az bicep build --file infra/main.bicep

# 4. ابتدا به‌صورت محلی تست کنید
npm run build
npm run test

# 5. از استقرار شبیه‌سازی‌شده (dry-run) استفاده کنید
azd provision --preview
```

### تنظیم مانیتورینگ
```bash
# فعال‌سازی Application Insights
# به main.bicep اضافه کنید:
resource appInsights 'Microsoft.Insights/components@2020-02-02' = {
  // ... configuration
}

# پیکربندی هشدارها
az monitor metrics alert create \
  --name "High CPU Usage" \
  --resource-group myrg \
  --scopes /subscriptions/{id}/resourceGroups/myrg/providers/Microsoft.Web/sites/myapp \
  --condition "avg Percentage CPU > 80"
```

### نگهداری منظم
```bash
# بررسی‌های سلامت هفتگی
./scripts/health-check.sh

# بازبینی هزینه‌های ماهانه
az consumption usage list --billing-period-name 202401

# بازبینی امنیتی سه‌ماهه
az security assessment list --resource-group myrg
```

## منابع مرتبط

- [راهنمای رفع اشکال](debugging.md) - تکنیک‌های پیشرفته عیب‌یابی
- [فراهم‌سازی منابع](../chapter-04-infrastructure/provisioning.md) - عیب‌یابی زیرساخت
- [برنامه‌ریزی ظرفیت](../chapter-06-pre-deployment/capacity-planning.md) - راهنمای برنامه‌ریزی منابع
- [انتخاب SKU](../chapter-06-pre-deployment/sku-selection.md) - توصیه‌های سطح سرویس

---

**نکته**: این راهنما را نشانک‌گذاری کنید و هر زمان با مشکلی مواجه شدید به آن مراجعه کنید. بیشتر مشکلات قبلاً مشاهده شده و راه‌حل‌های شناخته‌شده‌ای دارند!

---

**ناوبری**
- **درس قبلی**: [فراهم‌سازی منابع](../chapter-04-infrastructure/provisioning.md)
- **درس بعدی**: [راهنمای رفع اشکال](debugging.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
سلب مسئولیت:
این سند با استفاده از سرویس ترجمهٔ هوش مصنوعی [Co-op Translator](https://github.com/Azure/co-op-translator) ترجمه شده است. در حالی که ما در تلاش برای دقت هستیم، لطفاً توجه داشته باشید که ترجمه‌های خودکار ممکن است شامل خطاها یا نادرستی‌هایی باشند. سند اصلی به زبان بومی آن باید به‌عنوان مرجع معتبر در نظر گرفته شود. برای اطلاعات حیاتی یا بسیار مهم، توصیه می‌شود از ترجمهٔ حرفه‌ای انسانی استفاده شود. ما در قبال هرگونه سوءتفاهم یا تفسیر نادرست ناشی از استفاده از این ترجمه مسئول نیستیم.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->