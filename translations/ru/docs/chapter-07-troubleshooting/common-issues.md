# Общие проблемы и решения

**Chapter Navigation:**
- **📚 Course Home**: [AZD для начинающих](../../README.md)
- **📖 Current Chapter**: Глава 7 - Устранение неполадок и отладка
- **⬅️ Previous Chapter**: [Глава 6: Проверки перед развёртыванием](../chapter-06-pre-deployment/preflight-checks.md)
- **➡️ Next**: [Руководство по отладке](debugging.md)
- **🚀 Next Chapter**: [Глава 8: Производственные и корпоративные шаблоны](../chapter-08-production/production-ai-practices.md)

## Введение

Это подробное руководство по устранению неполадок охватывает наиболее часто встречающиеся проблемы при использовании Azure Developer CLI. Научитесь диагностировать, устранять и решать общие проблемы с аутентификацией, развертыванием, созданием инфраструктуры и конфигурацией приложений. Для каждой проблемы приведены симптомы, первопричины и пошаговые процедуры решения.

## Цели обучения

Пройдя это руководство, вы:
- Овладеете методами диагностики проблем Azure Developer CLI
- Поймёте распространённые проблемы с аутентификацией и правами доступа и способы их решения
- Научитесь устранять ошибки развертывания, сбои при создании инфраструктуры и проблемы конфигурации
- Реализуете проактивные стратегии мониторинга и отладки
- Примените системный подход к решению сложных проблем
- Настроите корректный сбор логов и мониторинг для предотвращения будущих проблем

## Результаты обучения

По завершении вы сможете:
- Диагностировать проблемы Azure Developer CLI с помощью встроенных инструментов диагностики
- Самостоятельно решать проблемы, связанные с аутентификацией, подписками и правами доступа
- Эффективно устранять сбои развертывания и ошибки при provisioning инфраструктуры
- Отлаживать проблемы с конфигурацией приложений и особенностями окружения
- Внедрять мониторинг и оповещения для проактивного обнаружения потенциальных проблем
- Применять лучшие практики для логирования, отладки и рабочих процессов по решению проблем

## Быстрая диагностика

Прежде чем переходить к конкретным проблемам, выполните эти команды, чтобы собрать диагностическую информацию:

```bash
# Проверить версию и состояние azd
azd version
azd config list

# Проверить аутентификацию в Azure
az account show
az account list

# Проверить текущее окружение
azd env show
azd env get-values

# Включить отладочное логирование
export AZD_DEBUG=true
azd <command> --debug
```

## Проблемы с аутентификацией

### Проблема: "Failed to get access token"
**Симптомы:**
- `azd up` завершается с ошибками аутентификации
- Команды возвращают "unauthorized" или "access denied"

**Решения:**
```bash
# 1. Повторно аутентифицируйтесь в Azure CLI
az login
az account show

# 2. Очистите кешированные учетные данные
az account clear
az login

# 3. Используйте поток кода устройства (для систем без графического интерфейса)
az login --use-device-code

# 4. Установите конкретную подписку
az account set --subscription "your-subscription-id"
azd config set defaults.subscription "your-subscription-id"
```

### Проблема: "Insufficient privileges" во время развертывания
**Симптомы:**
- Развёртывание завершается с ошибками прав доступа
- Невозможно создать определённые ресурсы Azure

**Решения:**
```bash
# 1. Проверьте назначенные вам роли в Azure
az role assignment list --assignee $(az account show --query user.name -o tsv)

# 2. Убедитесь, что у вас есть необходимые роли
# - Участник (Contributor) (для создания ресурсов)
# - Администратор доступа пользователей (User Access Administrator) (для назначения ролей)

# 3. Обратитесь к администратору Azure для получения необходимых разрешений
```

### Проблема: проблемы с мультиарендной аутентификацией
**Решения:**
```bash
# 1. Войти под конкретным тенантом
az login --tenant "your-tenant-id"

# 2. Установить тенант в конфигурации
azd config set auth.tenantId "your-tenant-id"

# 3. Очистить кэш тенантов при переключении
az account clear
```

## 🏗️ Ошибки при развертывании инфраструктуры

### Проблема: Конфликты имён ресурсов
**Симптомы:**
- Ошибки "The resource name already exists"
- Развёртывание прерывается при создании ресурсов

**Решения:**
```bash
# 1. Используйте уникальные имена ресурсов с токенами
# В вашем шаблоне Bicep:
var resourceToken = toLower(uniqueString(subscription().id, environmentName, location))
name: '${applicationName}-${resourceToken}'

# 2. Измените имя окружения
azd env new my-app-dev-$(whoami)-$(date +%s)

# 3. Удалите существующие ресурсы
azd down --force --purge
```

### Проблема: Регион/локация недоступна
**Симптомы:**
- "The location 'xyz' is not available for resource type"
- Некоторые SKU недоступны в выбранном регионе

**Решения:**
```bash
# 1. Проверьте доступные местоположения для типов ресурсов
az provider show --namespace Microsoft.Web --query "resourceTypes[?resourceType=='sites'].locations" -o table

# 2. Используйте обычно доступные регионы
azd config set defaults.location eastus2
# или
azd env set AZURE_LOCATION eastus2

# 3. Проверьте доступность сервиса по регионам
# Посетите: https://azure.microsoft.com/global-infrastructure/services/
```

### Проблема: Превышение квот
**Симптомы:**
- "Quota exceeded for resource type"
- "Maximum number of resources reached"

**Решения:**
```bash
# 1. Проверьте текущее использование квоты
az vm list-usage --location eastus2 -o table

# 2. Запросите увеличение квоты через портал Azure
# Перейдите в: Подписки > Использование и квоты

# 3. Используйте меньшие SKU для разработки
# В main.parameters.json:
{
  "appServiceSku": {
    "value": "B1"  // Instead of P1v3
  }
}

# 4. Удалите неиспользуемые ресурсы
az resource list --query "[?contains(name, 'unused')]" -o table
```

### Проблема: Ошибки шаблона Bicep
**Симптомы:**
- Ошибки валидации шаблона
- Синтаксические ошибки в файлах Bicep

**Решения:**
```bash
# 1. Проверить синтаксис Bicep
az bicep build --file infra/main.bicep

# 2. Использовать линтер Bicep
az bicep lint --file infra/main.bicep

# 3. Проверить синтаксис файла параметров
cat infra/main.parameters.json | jq '.'

# 4. Просмотреть изменения развертывания
azd provision --preview
```

## 🚀 Сбои при развертывании

### Проблема: Ошибки сборки
**Симптомы:**
- Приложение не собирается во время развертывания
- Ошибки установки пакетов

**Решения:**
```bash
# 1. Проверить вывод сборки с флагом отладки
azd deploy --service web --debug

# 2. Просмотреть статус развернутой службы
azd show

# 3. Протестировать сборку локально
cd src/web
npm install
npm run build

# 3. Проверить совместимость версий Node.js и Python
node --version  # Должно соответствовать настройкам в azure.yaml
python --version

# 4. Очистить кэш сборки
rm -rf node_modules package-lock.json
npm install

# 5. Проверить Dockerfile, если используются контейнеры
docker build -t test-image .
docker run --rm test-image
```

### Проблема: Ошибки развертывания контейнеров
**Симптомы:**
- Контейнерные приложения не запускаются
- Ошибки при pull образа

**Решения:**
```bash
# 1. Протестировать локальную сборку Docker
docker build -t my-app:latest .
docker run --rm -p 3000:3000 my-app:latest

# 2. Проверить журналы контейнера с помощью Azure CLI
az containerapp logs show --name my-app --resource-group my-rg --follow

# 3. Отслеживать приложение с помощью azd
azd monitor --logs

# 3. Проверить доступ к реестру контейнеров
az acr login --name myregistry

# 4. Проверить конфигурацию контейнерного приложения
az containerapp show --name my-app --resource-group my-rg
```

### Проблема: Ошибки подключения к базе данных
**Симптомы:**
- Приложение не может подключиться к базе данных
- Истекает время ожидания соединения

**Решения:**
```bash
# 1. Проверьте правила брандмауэра базы данных
az postgres flexible-server firewall-rule list --name mydb --resource-group myrg

# 2. Протестируйте подключение из приложения
# Временно добавьте в ваше приложение:
curl -v telnet://mydb.postgres.database.azure.com:5432

# 3. Проверьте формат строки подключения
azd env get-values | grep DATABASE

# 4. Проверьте статус сервера базы данных
az postgres flexible-server show --name mydb --resource-group myrg --query state
```

## 🔧 Проблемы конфигурации

### Проблема: Переменные окружения не работают
**Симптомы:**
- Приложение не может прочитать значения конфигурации
- Переменные окружения кажутся пустыми

**Решения:**
```bash
# 1. Убедитесь, что переменные окружения установлены
azd env get-values
azd env get DATABASE_URL

# 2. Проверьте имена переменных в azure.yaml
cat azure.yaml | grep -A 5 env:

# 3. Перезапустите приложение
azd deploy --service web

# 4. Проверьте конфигурацию службы приложения
az webapp config appsettings list --name myapp --resource-group myrg
```

### Проблема: Проблемы с SSL/TLS сертификатами
**Симптомы:**
- HTTPS не работает
- Ошибки валидации сертификата

**Решения:**
```bash
# 1. Проверить состояние SSL-сертификата
az webapp config ssl list --resource-group myrg

# 2. Включить только HTTPS
az webapp update --name myapp --resource-group myrg --https-only true

# 3. Добавить собственный домен (при необходимости)
az webapp config hostname add --webapp-name myapp --resource-group myrg --hostname mydomain.com
```

### Проблема: Проблемы с конфигурацией CORS
**Симптомы:**
- Фронтенд не может вызывать API
- Браузер блокирует кросс-доменный запрос

**Решения:**
```bash
# 1. Настроить CORS для службы приложений
az webapp cors add --name myapi --resource-group myrg --allowed-origins https://myapp.azurewebsites.net

# 2. Обновить API для обработки CORS
# В Express.js:
app.use(cors({
  origin: process.env.FRONTEND_URL,
  credentials: true
}));

# 3. Проверьте, работает ли приложение по правильным URL-адресам
azd show
```

## 🌍 Проблемы управления окружениями

### Проблема: Проблемы при переключении окружений
**Симптомы:**
- Используется неверное окружение
- Конфигурация не переключается корректно

**Решения:**
```bash
# 1. Перечислить все окружения
azd env list

# 2. Явно выбрать окружение
azd env select production

# 3. Проверить текущее окружение
azd env show

# 4. Создать новое окружение, если оно повреждено
azd env new production-new
azd env select production-new
```

### Проблема: Повреждение окружения
**Симптомы:**
- Окружение показывает некорректное состояние
- Ресурсы не соответствуют конфигурации

**Решения:**
```bash
# 1. Обновить состояние окружения
azd env refresh

# 2. Сбросить конфигурацию окружения
azd env new production-reset
# Скопировать необходимые переменные окружения
azd env set DATABASE_URL "your-value"

# 3. Импортировать существующие ресурсы (если возможно)
# Вручную обновите файл .azure/production/config.json, указав идентификаторы ресурсов
```

## 🔍 Проблемы производительности

### Проблема: Долгое время развертывания
**Симптомы:**
- Развёртывания занимают слишком много времени
- Таймауты во время развертывания

**Решения:**
```bash
# 1. Развёртывайте конкретные сервисы для более быстрой итерации
azd deploy --service web
azd deploy --service api

# 2. Используйте развертывание только кода, когда инфраструктура не меняется
azd deploy  # Быстрее, чем azd up

# 3. Оптимизируйте процесс сборки
# В package.json:
"scripts": {
  "build": "webpack --mode=production --optimize-minimize"
}

# 4. Проверьте расположение ресурсов (используйте тот же регион)
azd config set defaults.location eastus2
```

### Проблема: Проблемы с производительностью приложения
**Симптомы:**
- Медленные ответы
- Высокое использование ресурсов

**Решения:**
```bash
# 1. Масштабируйте ресурсы
# Обновите SKU в main.parameters.json:
"appServiceSku": {
  "value": "S2"  // Scale up from B1
}

# 2. Включите мониторинг Application Insights
azd monitor --overview

# 3. Проверьте журналы приложения в Azure
az webapp log tail --name myapp --resource-group myrg
# или для Container Apps:
az containerapp logs show --name myapp --resource-group myrg --follow

# 4. Внедрите кэширование
# Добавьте Redis-кэш в вашу инфраструктуру
```

## 🛠️ Инструменты и команды для устранения неполадок

### Команды для отладки
```bash
# Всесторонняя отладка
export AZD_DEBUG=true
azd up --debug 2>&1 | tee debug.log

# Проверить версию azd
azd version

# Просмотреть текущую конфигурацию
azd config list

# Проверить подключение
curl -v https://myapp.azurewebsites.net/health
```

### Анализ логов
```bash
# Журналы приложения через Azure CLI
az webapp log tail --name myapp --resource-group myrg

# Мониторинг приложения с помощью azd
azd monitor --logs
azd monitor --live

# Журналы ресурсов Azure
az monitor activity-log list --resource-group myrg --start-time 2024-01-01 --max-events 50

# Журналы контейнеров (для Container Apps)
az containerapp logs show --name myapp --resource-group myrg --follow
```

### Исследование ресурсов
```bash
# Перечислить все ресурсы
az resource list --resource-group myrg -o table

# Проверить состояние ресурса
az webapp show --name myapp --resource-group myrg --query state

# Диагностика сети
az network watcher test-connectivity --source-resource myvm --dest-address myapp.azurewebsites.net --dest-port 443
```

## 🆘 Получение дополнительной помощи

### Когда следует эскалировать
- Проблемы с аутентификацией сохраняются после выполнения всех предложенных решений
- Проблемы с инфраструктурой на стороне служб Azure
- Вопросы, связанные с биллингом или подпиской
- Проблемы безопасности или инциденты

### Каналы поддержки
```bash
# 1. Проверьте состояние службы Azure Service Health
az rest --method get --uri "https://management.azure.com/subscriptions/{subscription-id}/providers/Microsoft.ResourceHealth/availabilityStatuses?api-version=2020-05-01"

# 2. Создайте обращение в службу поддержки Azure
# Перейдите по адресу: https://portal.azure.com -> Справка и поддержка

# 3. Ресурсы сообщества
# - Stack Overflow: метка azure-developer-cli
# - Issues на GitHub: https://github.com/Azure/azure-dev/issues
# - Microsoft Q&A: https://learn.microsoft.com/en-us/answers/
```

### Информация для сбора
Перед обращением в поддержку соберите:
- `azd version` вывод
- `azd config list` вывод
- `azd show` вывод (текущий статус развёртывания)
- Сообщения об ошибках (полный текст)
- Шаги для воспроизведения проблемы
- Детали окружения (`azd env show`)
- Хронология возникновения проблемы

### Скрипт сбора логов
```bash
#!/bin/bash
# collect-debug-info.sh

echo "Collecting azd debug information..."
mkdir -p debug-logs

echo "System Information:" > debug-logs/system-info.txt
azd version >> debug-logs/system-info.txt
az --version >> debug-logs/system-info.txt

echo "Configuration:" > debug-logs/config.txt
azd config list >> debug-logs/config.txt
azd env show >> debug-logs/config.txt
azd env get-values >> debug-logs/config.txt

echo "Current deployment status:" > debug-logs/status.txt
azd show >> debug-logs/status.txt

echo "Debug information collected in debug-logs/"
```

## 📊 Предотвращение проблем

### Контрольный список перед развёртыванием
```bash
# 1. Проверить аутентификацию
az account show

# 2. Проверить квоты и лимиты
az vm list-usage --location eastus2

# 3. Проверить шаблоны
az bicep build --file infra/main.bicep

# 4. Сначала протестировать локально
npm run build
npm run test

# 5. Использовать пробные развертывания
azd provision --preview
```

### Настройка мониторинга
```bash
# Включить Application Insights
# Добавить в main.bicep:
resource appInsights 'Microsoft.Insights/components@2020-02-02' = {
  // ... configuration
}

# Настроить оповещения
az monitor metrics alert create \
  --name "High CPU Usage" \
  --resource-group myrg \
  --scopes /subscriptions/{id}/resourceGroups/myrg/providers/Microsoft.Web/sites/myapp \
  --condition "avg Percentage CPU > 80"
```

### Регулярное обслуживание
```bash
# Еженедельные проверки состояния
./scripts/health-check.sh

# Ежемесячный обзор затрат
az consumption usage list --billing-period-name 202401

# Квартальный обзор безопасности
az security assessment list --resource-group myrg
```

## Связанные ресурсы

- [Руководство по отладке](debugging.md) - Продвинутые методы отладки
- [Развёртывание ресурсов](../chapter-04-infrastructure/provisioning.md) - Устранение неполадок инфраструктуры
- [Планирование ёмкости](../chapter-06-pre-deployment/capacity-planning.md) - Руководство по планированию ресурсов
- [Выбор SKU](../chapter-06-pre-deployment/sku-selection.md) - Рекомендации по выбору уровня сервиса

---

**Совет**: Добавьте это руководство в закладки и обращайтесь к нему всякий раз, когда сталкиваетесь с проблемами. Большинство проблем уже встречались ранее и имеют проверенные решения!

---

**Навигация**
- **Previous Lesson**: [Развёртывание ресурсов](../chapter-04-infrastructure/provisioning.md)
- **Next Lesson**: [Руководство по отладке](debugging.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Отказ от ответственности**:
Этот документ был переведён с помощью сервиса машинного перевода на основе ИИ [Co-op Translator](https://github.com/Azure/co-op-translator). Хотя мы прилагаем усилия для обеспечения точности, обратите внимание, что автоматические переводы могут содержать ошибки или неточности. Оригинальный документ на его исходном языке следует считать авторитетным источником. Для критически важной информации рекомендуется привлекать профессионального переводчика. Мы не несем ответственности за любые недоразумения или неверные толкования, возникшие в результате использования этого перевода.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->