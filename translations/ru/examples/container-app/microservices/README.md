# Архитектура микросервисов - пример контейнерного приложения

⏱️ **Ориентировочное время**: 25-35 минут | 💰 **Ориентировочная стоимость**: ~$50-100/месяц | ⭐ **Сложность**: Продвинутый уровень

**Упрощённая, но функциональная** архитектура микросервисов, развернутая в Azure Container Apps с использованием AZD CLI. В этом примере демонстрируются взаимодействие между сервисами, оркестрация контейнеров и мониторинг на практике с использованием двух сервисов.

> **📚 Подход к обучению**: Этот пример начинается с минимальной архитектуры из 2 сервисов (API Gateway + Backend Service), которую вы сможете развернуть и изучить. После освоения основ мы предлагаем руководство по расширению до полноценной экосистемы микросервисов.

## Что вы изучите

Выполнив этот пример, вы сможете:
- Развернуть несколько контейнеров в Azure Container Apps
- Реализовать взаимодействие между сервисами через внутренние сети
- Настроить масштабирование и проверки работоспособности в зависимости от окружения
- Мониторить распределённые приложения с помощью Application Insights
- Понять шаблоны развертывания микросервисов и лучшие практики
- Освоить постепенное расширение от простой к сложной архитектуре

## Архитектура

### Фаза 1: Что мы строим (включено в этот пример)

```
                    ┌─────────────────────────────┐
                    │         Internet            │
                    └──────────────┬──────────────┘
                                   │
                                   │ HTTPS
                                   │
                    ┌──────────────▼──────────────┐
                    │      API Gateway            │
                    │   (Node.js Container)       │
                    │   - Routes requests         │
                    │   - Health checks           │
                    │   - Request logging         │
                    └──────────────┬──────────────┘
                                   │
                                   │ HTTP (internal)
                                   │
                    ┌──────────────▼──────────────┐
                    │    Product Service          │
                    │   (Python Container)        │
                    │   - Product CRUD            │
                    │   - In-memory data store    │
                    │   - REST API                │
                    └──────────────┬──────────────┘
                                   │
                    ┌──────────────▼──────────────┐
                    │   Application Insights      │
                    │   (Monitoring & Logs)       │
                    └─────────────────────────────┘
```

**Почему начинать просто?**
- ✅ Быстро развернуть и понять (25-35 минут)
- ✅ Изучить основные паттерны микросервисов без лишней сложности
- ✅ Рабочий код для модификаций и экспериментов
- ✅ Низкая стоимость для обучения (~50-100$/месяц против 300-1400$/месяц)
- ✅ Накопить уверенность перед добавлением баз данных и очередей сообщений

**Аналогия**: Это похоже на обучение вождению. Начинаете с пустой парковки (2 сервиса), осваиваете основы и переходите к городскому движению (5+ сервисов с БД).

### Фаза 2: Будущее расширение (референсная архитектура)

После освоения архитектуры с 2 сервисами вы можете расширить её до:

```
Full Architecture (Not Included - For Reference)
├── API Gateway (✅ Included)
├── Product Service (✅ Included)
├── Order Service (🔜 Add next)
├── User Service (🔜 Add next)
├── Notification Service (🔜 Add last)
├── Azure Service Bus (🔜 For async communication)
├── Cosmos DB (🔜 For product persistence)
├── Azure SQL (🔜 For order management)
└── Azure Storage (🔜 For file storage)
```

Смотрите раздел "Руководство по расширению" в конце для пошаговых инструкций.

## Включённые функции

✅ **Обнаружение сервисов**: Автоматический DNS-based discovery между контейнерами  
✅ **Балансировка нагрузки**: Встроенная балансировка между репликами  
✅ **Автоматическое масштабирование**: Независимое масштабирование по сервисам на основе HTTP-запросов  
✅ **Мониторинг здоровья**: Пробы liveness и readiness для обоих сервисов  
✅ **Распределённое логирование**: Централизованное логирование с Application Insights  
✅ **Внутренние сети**: Безопасное взаимодействие сервисов  
✅ **Оркестрация контейнеров**: Автоматическое развертывание и масштабирование  
✅ **Обновления без простоя**: Пошаговые обновления с управлением ревизиями  

## Требования

### Необходимые инструменты

Перед началом убедитесь, что у вас установлены:

1. **[Azure Developer CLI (azd)](https://learn.microsoft.com/azure/developer/azure-developer-cli/install-azd)** (версия 1.0.0 или выше)
   ```bash
   azd version
   # Ожидаемый вывод: azd версия 1.0.0 или выше
   ```

2. **[Azure CLI](https://learn.microsoft.com/cli/azure/install-azure-cli)** (версия 2.50.0 или выше)
   ```bash
   az --version
   # Ожидаемый вывод: azure-cli 2.50.0 или выше
   ```

3. **[Docker](https://www.docker.com/get-started)** (для локальной разработки/тестирования – опционально)
   ```bash
   docker --version
   # Ожидаемый результат: версия Docker 20.10 или выше
   ```

### Требования Azure

- Активная **подписка Azure** ([создать бесплатный аккаунт](https://azure.microsoft.com/free/))
- Права на создание ресурсов в подписке
- Роль **Contributor** в подписке или группе ресурсов

### Необходимые знания

Это пример **продвинутого уровня**. Рекомендуется иметь:
- Завершённый [простой пример Flask API](../../../../../examples/container-app/simple-flask-api) 
- Базовое понимание архитектуры микросервисов
- Знание REST API и HTTP
- Представление о контейнерах

**Новичок в Container Apps?** Начните с [простого примера Flask API](../../../../../examples/container-app/simple-flask-api), чтобы освоить основы.

## Быстрый старт (пошагово)

### Шаг 1: Клонируйте репозиторий и перейдите в папку

```bash
git clone https://github.com/microsoft/AZD-for-beginners.git
cd AZD-for-beginners/examples/container-app/microservices
```

**✓ Проверка успеха**: Убедитесь, что видите файл `azure.yaml`:
```bash
ls
# Ожидается: README.md, azure.yaml, infra/, src/
```

### Шаг 2: Аутентификация в Azure

```bash
azd auth login
```

Откроется браузер для входа в Azure. Войдите, используя свои учетные данные.

**✓ Проверка успеха**: Вы должны увидеть:
```
Logged in to Azure.
```

### Шаг 3: Инициализация окружения

```bash
azd init
```

**Вводимые значения**:
- **Имя окружения**: Введите короткое имя (например, `microservices-dev`)
- **Подписка Azure**: Выберите подписку
- **Регион Azure**: Выберите регион (напр., `eastus`, `westeurope`)

**✓ Проверка успеха**: Вы увидите:
```
SUCCESS: New project initialized!
```

### Шаг 4: Развертывание инфраструктуры и сервисов

```bash
azd up
```

**Что происходит** (занимает 8-12 минут):
1. Создаётся среда Container Apps
2. Создаётся Application Insights для мониторинга
3. Строится контейнер API Gateway (Node.js)
4. Строится контейнер Product Service (Python)
5. Оба контейнера развертываются в Azure
6. Настраиваются сеть и проверки здоровья
7. Настраивается мониторинг и логирование

**✓ Проверка успеха**: Вы должны увидеть:
```
SUCCESS: Your application was deployed to Azure in X minutes Y seconds.
Endpoint: https://api-gateway-<unique-id>.azurecontainerapps.io
```

**⏱️ Время**: 8-12 минут

### Шаг 5: Тестирование развертывания

```bash
# Получить конечную точку шлюза
GATEWAY_URL=$(azd env get-values | grep API_GATEWAY_URL | cut -d '=' -f2 | tr -d '"')

# Проверить состояние API Gateway
curl $GATEWAY_URL/health

# Ожидаемый результат:
# {"status":"healthy","service":"api-gateway","timestamp":"2025-11-19T10:30:00Z"}
```

**Проверьте product service через gateway**:
```bash
# Список продуктов
curl $GATEWAY_URL/api/products

# Ожидаемый вывод:
# [
#   {"id":1,"name":"Ноутбук","price":999.99,"stock":50},
#   {"id":2,"name":"Мышь","price":29.99,"stock":200},
#   {"id":3,"name":"Клавиатура","price":79.99,"stock":150}
# ]
```

**✓ Проверка успеха**: Оба эндпоинта возвращают JSON без ошибок.

---

**🎉 Поздравляем!** Вы развернули архитектуру микросервисов в Azure!

## Структура проекта

Вся реализация включена — это полный работоспособный пример:

```
microservices/
│
├── README.md                         # This file
├── azure.yaml                        # AZD configuration
├── .gitignore                        # Git ignore patterns
│
├── infra/                           # Infrastructure as Code (Bicep)
│   ├── main.bicep                   # Main orchestration
│   ├── abbreviations.json           # Naming conventions
│   ├── core/                        # Shared infrastructure
│   │   ├── container-apps-environment.bicep  # Container environment + registry
│   │   └── monitor.bicep            # Application Insights + Log Analytics
│   └── app/                         # Service definitions
│       ├── api-gateway.bicep        # API Gateway container app
│       └── product-service.bicep    # Product Service container app
│
└── src/                             # Application source code
    ├── api-gateway/                 # Node.js API Gateway
    │   ├── app.js                   # Express server with routing
    │   ├── package.json             # Node dependencies
    │   └── Dockerfile               # Container definition
    └── product-service/             # Python Product Service
        ├── main.py                  # Flask API with product data
        ├── requirements.txt         # Python dependencies
        └── Dockerfile               # Container definition
```

**Назначение компонентов:**

**Инфраструктура (infra/)**:
- `main.bicep`: Оркеструет все Azure-ресурсы и зависимости
- `core/container-apps-environment.bicep`: Создаёт среду Container Apps и Azure Container Registry
- `core/monitor.bicep`: Настраивает Application Insights для распределённого логирования
- `app/*.bicep`: Определения отдельных контейнерных приложений с масштабированием и проверками здоровья

**API Gateway (src/api-gateway/)**:
- Внешний сервис, который маршрутизирует запросы к бэкенд-сервисам
- Реализует логирование, обработку ошибок и форвардинг запросов
- Демонстрирует HTTP-взаимодействие между сервисами

**Product Service (src/product-service/)**:
- Внутренний сервис с каталогом продуктов (хранится в оперативной памяти)
- REST API с проверками работоспособности
- Пример паттерна бэкенд-маркоусервиса

## Обзор сервисов

### API Gateway (Node.js/Express)

**Порт**: 8080  
**Доступ**: Публичный (внешний доступ)  
**Назначение**: Маршрутизирует входящие запросы к нужным бэкенд-сервисам  

**Эндпоинты**:
- `GET /` – информация о сервисе
- `GET /health` – проверка здоровья
- `GET /api/products` – запрос к product service (получить список)
- `GET /api/products/:id` – запрос к product service (по ID)

**Основные особенности**:
- Маршрутизация запросов с axios
- Централизованное логирование
- Обработка ошибок и управление таймаутами
- Обнаружение сервисов через переменные среды
- Интеграция с Application Insights

**Пример кода** (`src/api-gateway/app.js`):
```javascript
// Внутреннее взаимодействие сервиса
app.get('/api/products', async (req, res) => {
  const response = await axios.get(`${PRODUCT_SERVICE_URL}/products`);
  res.json(response.data);
});
```

### Product Service (Python/Flask)

**Порт**: 8000  
**Доступ**: Внутренний (нет внешнего входа)  
**Назначение**: Управление каталогом продуктов (данные в памяти)  

**Эндпоинты**:
- `GET /` – информация о сервисе
- `GET /health` – проверка здоровья
- `GET /products` – получить список всех продуктов
- `GET /products/<id>` – получить продукт по ID

**Основные особенности**:
- REST API с Flask
- Хранение продуктов в памяти (простое, без БД)
- Мониторинг здоровья с пробами
- Структурированное логирование
- Интеграция с Application Insights

**Модель данных**:
```python
{
  "id": 1,
  "name": "Laptop",
  "description": "High-performance laptop",
  "price": 999.99,
  "stock": 50
}
```

**Почему только внутренний доступ?**
Product service не выставлен публично. Все запросы проходят через API Gateway, который обеспечивает:
- Безопасность: Контролируемая точка доступа
- Гибкость: Можно менять бэкенд без влияния на клиентов
- Мониторинг: Центральное логирование запросов

## Понимание коммуникации сервисов

### Как сервисы общаются друг с другом

В этом примере API Gateway взаимодействует с Product Service через **внутренние HTTP-запросы**:

```javascript
// API Gateway (src/api-gateway/app.js)
const PRODUCT_SERVICE_URL = process.env.PRODUCT_SERVICE_URL;

// Сделать внутренний HTTP-запрос
const response = await axios.get(`${PRODUCT_SERVICE_URL}/products`);
```

**Главные моменты**:

1. **Обнаружение через DNS**: Container Apps автоматически предоставляет DNS для внутренних сервисов
   - Полное имя Product Service: `product-service.internal.<environment>.azurecontainerapps.io`
   - Упрощённо: `http://product-service` (разрешается Container Apps)

2. **Без публичного доступа**: В Bicep у Product Service выставлено `external: false`
   - Доступен только внутри среды Container Apps
   - Недоступен из интернета

3. **Переменные окружения**: URL сервисов передаются при развертывании
   - Bicep передаёт внутренний FQDN в gateway
   - Нет жёстко закодированных URL в коде

**Аналогия**: Это как офис. API Gateway – ресепшн, публичная точка, а Product Service – внутренний кабинет. Чтобы попасть в кабинет, нужно сначала пройти через ресепшн.

## Варианты развертывания

### Полное развертывание (рекомендуется)

```bash
# Развернуть инфраструктуру и оба сервиса
azd up
```

Разворачивает:
1. Среду Container Apps
2. Application Insights
3. Container Registry
4. Контейнер API Gateway
5. Контейнер Product Service

**Время**: 8-12 минут

### Развернуть отдельный сервис

```bash
# Развернуть только один сервис (после начального azd up)
azd deploy api-gateway

# Или развернуть сервис продукта
azd deploy product-service
```

**Случай использования**: Когда вы обновили код одного сервиса и хотите развернуть только его.

### Обновить конфигурацию

```bash
# Изменить параметры масштабирования
azd env set GATEWAY_MAX_REPLICAS 30

# Переустановить с новой конфигурацией
azd up
```

## Конфигурация

### Настройка масштабирования

Оба сервиса настроены на автоскейлинг по HTTP в их Bicep-файлах:

**API Gateway**:
- Мин. число реплик: 2 (минимум 2 всегда для доступности)
- Макс. число реплик: 20
- Триггер масштабирования: 50 одновременных запросов на реплику

**Product Service**:
- Мин. число реплик: 1 (может масштабироваться до нуля)
- Макс. число реплик: 10
- Триггер масштабирования: 100 одновременных запросов на реплику

**Настроить масштабирование** (в `infra/app/*.bicep`):
```bicep
scale: {
  minReplicas: 1
  maxReplicas: 10
  rules: [
    {
      name: 'http-scale-rule'
      http: {
        metadata: {
          concurrentRequests: '100'  // Adjust this
        }
      }
    }
  ]
}
```

### Распределение ресурсов

**API Gateway**:
- CPU: 1.0 vCPU
- Память: 2 GiB
- Причина: Обрабатывает весь внешний трафик

**Product Service**:
- CPU: 0.5 vCPU
- Память: 1 GiB
- Причина: Легковесные операции в памяти

### Проверки состояния

Оба сервиса включают пробы liveness и readiness:

```bicep
probes: [
  {
    type: 'Liveness'
    httpGet: {
      path: '/health'
      port: 8080
    }
    initialDelaySeconds: 10
    periodSeconds: 30
  }
  {
    type: 'Readiness'
    httpGet: {
      path: '/health'
      port: 8080
    }
    initialDelaySeconds: 5
    periodSeconds: 10
  }
]
```

**Что это значит**:
- **Liveness**: При сбое проверки Container Apps перезапускает контейнер
- **Readiness**: Если сервис не готов, трафик на эту реплику не маршрутизируется



## Мониторинг и наблюдаемость

### Просмотр логов сервисов

```bash
# Просмотр журналов с помощью azd monitor
azd monitor --logs

# Или используйте Azure CLI для конкретных контейнерных приложений:
# Потоковая передача журналов из API Gateway
az containerapp logs show --name api-gateway --resource-group $RG_NAME --follow

# Просмотр последних журналов службы продукта
az containerapp logs show --name product-service --resource-group $RG_NAME --tail 100
```

**Ожидаемый вывод**:
```
[api-gateway] API Gateway listening on port 8080
[api-gateway] Product Service URL: http://product-service
[api-gateway] GET /api/products 200 - 45ms
[product-service] Retrieved 5 products
```

### Запросы к Application Insights

Откройте Application Insights в Azure Portal и выполните эти запросы:

**Найти медленные запросы**:
```kusto
requests
| where timestamp > ago(1h)
| where duration > 1000  // Requests taking >1 second
| summarize count() by name, cloud_RoleName
| order by count_ desc
```

**Отследить вызовы между сервисами**:
```kusto
dependencies
| where timestamp > ago(1h)
| where type == "Http"
| project timestamp, name, target, duration, success
| order by timestamp desc
```

**Доля ошибок по сервисам**:
```kusto
exceptions
| where timestamp > ago(24h)
| summarize errorCount = count() by cloud_RoleName, type
| order by errorCount desc
```

**Объем запросов во времени**:
```kusto
requests
| where timestamp > ago(1h)
| summarize requestCount = count() by bin(timestamp, 5m), cloud_RoleName
| render timechart
```

### Доступ к панели мониторинга

```bash
# Получить сведения об Application Insights
azd env get-values | grep APPLICATIONINSIGHTS

# Открыть мониторинг в портале Azure
az monitor app-insights component show \
  --app $(azd env get-values | grep APPLICATIONINSIGHTS_CONNECTION_STRING | cut -d '=' -f2) \
  --resource-group $(azd env get-values | grep AZURE_RESOURCE_GROUP | cut -d '=' -f2) \
  --query "appId" -o tsv
```

### Метрические данные в реальном времени

1. Перейдите в Application Insights в Azure Portal
2. Нажмите "Live Metrics"
3. Посмотрите запросы, ошибки и производительность в реальном времени
4. Проверьте командой: `curl $(azd env get-values | grep API_GATEWAY_URL | cut -d '=' -f2 | tr -d '"')/api/products`

## Практические упражнения

[Примечание: Подробные пошаговые упражнения приведены выше в разделе "Практические упражнения" и включают проверку развертывания, изменение данных, тесты автоскейлинга, обработку ошибок и добавление третьего сервиса.]

## Анализ стоимости

### Ориентировочные ежемесячные расходы (для примера с 2 сервисами)

| Ресурс | Конфигурация | Ориентировочная стоимость |
|----------|--------------|---------------------------|
| API Gateway | 2-20 реплик, 1 vCPU, 2GB RAM | $30-150 |
| Product Service | 1-10 реплик, 0.5 vCPU, 1GB RAM | $15-75 |
| Container Registry | Базовый уровень | $5 |
| Application Insights | 1-2 ГБ/месяц | $5-10 |
| Log Analytics | 1 ГБ/месяц | $3 |
| **Итого** | | **$58-243/месяц** |

**Разбивка затрат по нагрузке**:
- **Низкая нагрузка** (тестирование/обучение): ~60$/месяц
- **Средняя нагрузка** (небольшое продакшн): ~120$/месяц
- **Высокая нагрузка** (пиковые периоды): ~240$/месяц

### Рекомендации по оптимизации затрат

1. **Масштабирование до нуля для разработки**:
   ```bicep
   scale: {
     minReplicas: 0  // Save $30-40/month when not in use
     maxReplicas: 10
   }
   ```

2. **Используйте модель оплаты по потреблению для Cosmos DB** (при добавлении):
   - Платите только за фактическое использование
   - Нет минимальных платежей

3. **Настройте выборку в Application Insights**:
   ```javascript
   appInsights.defaultClient.config.samplingPercentage = 50; // Выборка 50% запросов
   ```

4. **Удаляйте ресурсы, когда они не нужны**:
   ```bash
   azd down
   ```

### Опции бесплатного уровня
Для обучения/тестирования рассмотрите:  
- Используйте бесплатные кредиты Azure (первые 30 дней)  
- Минимизируйте количество реплик  
- Удаляйте после тестирования (чтобы избежать постоянных расходов)  

---

## Очистка

Чтобы избежать постоянных расходов, удалите все ресурсы:  

```bash
azd down --force --purge
```
  
**Подтверждение:**  
```
? Total resources to delete: 6, are you sure you want to continue? (y/N)
```
  
Введите `y` для подтверждения.  

**Что удаляется**:  
- Окружение Container Apps  
- Оба Container Apps (gateway и сервис продукта)  
- Контейнерный реестр  
- Application Insights  
- Рабочее пространство Log Analytics  
- Группа ресурсов  

**✓ Проверка очистки**:  
```bash
az group list --query "[?starts_with(name,'rg-microservices')]" --output table
```
  
Должно вернуть пустой результат.  

---

## Руководство по расширению: от 2 до 5+ сервисов

Освоив архитектуру из 2 сервисов, можно расширять следующим образом:  

### Фаза 1: Добавление базы данных (следующий шаг)

**Добавьте Cosmos DB для сервиса продукта**:  

1. Создайте `infra/core/cosmos.bicep`:  
   ```bicep
   resource cosmosAccount 'Microsoft.DocumentDB/databaseAccounts@2023-04-15' = {
     name: name
     location: location
     kind: 'GlobalDocumentDB'
     properties: {
       databaseAccountOfferType: 'Standard'
       locations: [{ locationName: location, failoverPriority: 0 }]
     }
   }
   ```
  
2. Обновите продуктовый сервис для использования Cosmos DB вместо хранения в памяти  

3. Оценочная дополнительная стоимость: около $25/месяц (serverless)  

### Фаза 2: Добавление третьего сервиса (управление заказами)

**Создайте сервис заказов**:  

1. Новая папка: `src/order-service/` (Python/Node.js/C#)  
2. Новый Bicep: `infra/app/order-service.bicep`  
3. Обновите API Gateway для маршрутизации `/api/orders`  
4. Добавьте Azure SQL Database для сохранения заказов  

**Архитектура становится**:  
```
API Gateway → Product Service (Cosmos DB)
           → Order Service (Azure SQL)
```
  
### Фаза 3: Добавление асинхронной коммуникации (Service Bus)

**Реализуйте событийно-ориентированную архитектуру**:  

1. Добавьте Azure Service Bus: `infra/core/servicebus.bicep`  
2. Сервис продукта публикует события "ProductCreated"  
3. Сервис заказов подписывается на события продукта  
4. Добавьте сервис уведомлений для обработки событий  

**Шаблон**: Запрос/ответ (HTTP) + событийно-ориентированный (Service Bus)  

### Фаза 4: Добавление аутентификации пользователей

**Реализуйте сервис пользователей**:  

1. Создайте `src/user-service/` (Go/Node.js)  
2. Добавьте Azure AD B2C или собственную JWT аутентификацию  
3. API Gateway проверяет токены  
4. Сервисы проверяют права пользователя  

### Фаза 5: Готовность к продакшену

**Добавьте эти компоненты**:  
- Azure Front Door (глобальный балансировщик нагрузки)  
- Azure Key Vault (управление секретами)  
- Azure Monitor Workbooks (настраиваемые дашборды)  
- CI/CD пайплайн (GitHub Actions)  
- Blue-Green деплойменты  
- Управляемая идентификация для всех сервисов  

**Полная стоимость продакшен-архитектуры**: примерно $300-1,400/месяц  

---

## Узнать больше

### Связанная документация  
- [Документация Azure Container Apps](https://learn.microsoft.com/azure/container-apps/)  
- [Руководство по архитектуре микросервисов](https://learn.microsoft.com/azure/architecture/guide/architecture-styles/microservices)  
- [Application Insights для распределённого трассирования](https://learn.microsoft.com/azure/azure-monitor/app/distributed-tracing)  
- [Документация Azure Developer CLI](https://learn.microsoft.com/azure/developer/azure-developer-cli/)  

### Следующие шаги в курсе  
- ← Предыдущий: [Простой Flask API](../../../../../examples/container-app/simple-flask-api) - пример для начинающих с одним контейнером  
- → Следующий: [Руководство по интеграции AI](../../../../../examples/docs/ai-foundry) - добавление AI возможностей  
- 🏠 [Домашняя страница курса](../../README.md)  

### Сравнение: когда что использовать

**Одноконтейнерное приложение** (пример Simple Flask API):  
- ✅ Простые приложения  
- ✅ Монолитная архитектура  
- ✅ Быстрое развертывание  
- ❌ Ограниченная масштабируемость  
- **Стоимость**: около $15-50/месяц  

**Микросервисы** (этот пример):  
- ✅ Сложные приложения  
- ✅ Независимое масштабирование сервисов  
- ✅ Автономность команд (разные сервисы — разные команды)  
- ❌ Более сложное управление  
- **Стоимость**: около $60-250/месяц  

**Kubernetes (AKS)**:  
- ✅ Максимальный контроль и гибкость  
- ✅ Мультиоблачная портируемость  
- ✅ Расширенные сетевые возможности  
- ❌ Требуется опыт Kubernetes  
- **Стоимость**: минимум $150-500/месяц  

**Рекомендация**: Начинайте с Container Apps (этот пример); переходите на AKS только при необходимости специфичных Kubernetes возможностей.  

---

## Часто задаваемые вопросы

**В: Почему всего 2 сервиса, а не 5+?**  
О: Для учебы. Освойте фундаментальные концепции (взаимодействие сервисов, мониторинг, масштабирование) на простом примере перед усложнением. Шаблоны применимы и к архитектурам с 100 сервисами.  

**В: Могу ли я добавить больше сервисов самостоятельно?**  
О: Конечно! Следуйте руководству по расширению выше. Каждый новый сервис — та же схема: создайте папку src, создайте Bicep, обновите azure.yaml, разверните.  

**В: Готово ли это к продакшену?**  
О: Это хорошая основа. Для продакшена добавьте: управляемую идентификацию, Key Vault, постоянные базы данных, CI/CD, мониторинг и оповещения, стратегию резервного копирования.  

**В: Почему не использовать Dapr или другую сервисную сетку?**  
О: Чтобы упростить обучение. После освоения нативной сетевой модели Container Apps можно добавить Dapr для продвинутых сценариев.  

**В: Как отлаживать локально?**  
О: Запускайте сервисы локально с помощью Docker:  
```bash
cd src/api-gateway
docker build -t local-gateway .
docker run -p 8080:8080 -e PRODUCT_SERVICE_URL=http://localhost:8000 local-gateway
```
  
**В: Можно ли использовать разные языки программирования?**  
О: Да! В примере Node.js (gateway) + Python (продуктовый сервис). Можно использовать любые поддерживаемые в контейнерах языки.  

**В: Что делать, если нет кредитов Azure?**  
О: Используйте бесплатный уровень Azure (первые 30 дней для новых аккаунтов) или развёртывайте для кратковременного тестирования и сразу удаляйте.  

---

> **🎓 Итог обучения**: Вы научились развёртывать многосервисную архитектуру с автоматическим масштабированием, внутренним сетевым взаимодействием, централизованным мониторингом и паттернами, готовыми к продакшену. Эта база готовит вас к сложным распределённым системам и корпоративным микросервисным архитектурам.  

**📚 Навигация по курсу:**  
- ← Предыдущий: [Простой Flask API](../../../../../examples/container-app/simple-flask-api)  
- → Следующий: [Пример интеграции базы данных](../../../../../examples/database-app)  
- 🏠 [Домашняя страница курса](../../../README.md)  
- 📖 [Лучшие практики Container Apps](../../../docs/chapter-04-infrastructure/deployment-guide.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Отказ от ответственности**:  
Этот документ был переведен с использованием сервиса автоматического перевода [Co-op Translator](https://github.com/Azure/co-op-translator). Несмотря на наши усилия по обеспечению точности, имейте в виду, что автоматический перевод может содержать ошибки или неточности. Оригинальный документ на его исходном языке должен рассматриваться как авторитетный источник. Для критически важной информации рекомендуется профессиональный перевод человеком. Мы не несем ответственности за любые недоразумения или неправильные толкования, возникшие в результате использования данного перевода.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->