# 2. Проверка шаблона

!!! tip "К КОНЦУ ЭТОГО МОДУЛЯ ВЫ СМОЖЕТЕ"

    - [ ] Анализировать архитектуру AI-решения
    - [ ] Понимать процесс развертывания AZD
    - [ ] Использовать GitHub Copilot для помощи с использованием AZD
    - [ ] **Lab 2:** Развертывать и валидировать шаблон AI Agents

---


## 1. Введение

[Azure Developer CLI](https://learn.microsoft.com/en-us/azure/developer/azure-developer-cli/) или `azd` — это инструмент командной строки с открытым исходным кодом, который упрощает рабочий процесс разработчика при создании и развертывании приложений в Azure. 

[AZD Templates](https://learn.microsoft.com/azure/developer/azure-developer-cli/azd-templates) — это стандартизированные репозитории, которые включают пример кода приложения, активы инфраструктуры как кода и файлы конфигурации `azd` для целостной архитектуры решения. Создание инфраструктуры становится таким же простым, как выполнение команды `azd provision`, а использование `azd up` позволяет создать инфраструктуру **и** развернуть ваше приложение за один шаг!

В результате начать разработку приложения может быть так же просто, как найти подходящий шаблон _AZD Starter_, который максимально соответствует вашим потребностям в приложении и инфраструктуре, а затем настроить репозиторий под ваши требования.

Прежде чем начать, убедитесь, что у вас установлен Azure Developer CLI.

1. Откройте терминал в VS Code и введите эту команду:

      ```bash title="" linenums="0"
      azd version
      ```

1. Вы должны увидеть что-то подобное!

      ```bash title="" linenums="0"
      azd version 1.19.0 (commit b3d68cea969b2bfbaa7b7fa289424428edb93e97)
      ```

**Теперь вы готовы выбрать и развернуть шаблон с помощью azd**

---

## 2. Выбор шаблона

Платформа Microsoft Foundry поставляется с [набором рекомендуемых шаблонов AZD](https://learn.microsoft.com/en-us/azure/ai-foundry/how-to/develop/ai-template-get-started), которые охватывают популярные сценарии решений, такие как _автоматизация рабочих процессов с несколькими агентами_ и _мультимодальная обработка контента_. Вы также можете найти эти шаблоны, посетив портал Microsoft Foundry.

1. Перейдите по адресу [https://ai.azure.com/templates](https://ai.azure.com/templates)
1. Войдите в портал Microsoft Foundry, когда появится запрос — вы увидите что-то похожее.

![Pick](../../../../../translated_images/ru/01-pick-template.60d2d5fff5ebc374.webp)


Опции **Basic** — это ваши стартовые шаблоны:

1. [ ] [Get Started with AI Chat](https://github.com/Azure-Samples/get-started-with-ai-chat), который разворачивает базовое чат-приложение _с вашими данными_ в Azure Container Apps. Используйте его, чтобы изучить базовый сценарий AI-чатбота.
1. [X] [Get Started with AI Agents](https://github.com/Azure-Samples/get-started-with-ai-agents), который также разворачивает стандартного AI агента (с Foundry Agents). Используйте его, чтобы познакомиться с агентными AI-решениями, включающими инструменты и модели.

Откройте вторую ссылку в новой вкладке браузера (или нажмите «Open in GitHub» на соответствующей карточке). Вы должны увидеть репозиторий для этого шаблона AZD. Потратьте минуту, чтобы изучить README. Архитектура приложения выглядит так:

![Arch](../../../../../translated_images/ru/architecture.8cec470ec15c65c7.webp)

---

## 3. Активация шаблона

Давайте попробуем развернуть этот шаблон и убедимся, что он действителен. Мы будем следовать указаниям в разделе [Getting Started](https://github.com/Azure-Samples/get-started-with-ai-agents?tab=readme-ov-file#getting-started).

1. Нажмите [на эту ссылку](https://github.com/codespaces/new/Azure-Samples/get-started-with-ai-agents) — подтвердите действие по умолчанию «Create codespace»
1. Откроется новая вкладка браузера — дождитесь полной загрузки сессии GitHub Codespaces
1. Откройте терминал VS Code в Codespaces и введите следующую команду:

   ```bash title="" linenums="0"
   azd up
   ```

Выполните шаги рабочего процесса, которые запустятся:

1. Вам будет предложено войти в Azure — следуйте инструкциям для аутентификации
1. Введите уникальное имя для окружения — например, я использовал `nitya-mshack-azd`
1. Это создаст папку `.azure/` — вы увидите подпапку с именем окружения
1. Вам будет предложено выбрать имя подписки — выберите по умолчанию
1. Вам будет предложено выбрать регион — используйте `East US 2`

Теперь дождитесь завершения создания инфраструктуры. **Это займет 10-15 минут**

1. По окончании в консоли появится сообщение ОБ УСПЕХЕ, похожее на это:
      ```bash title="" linenums="0"
      SUCCESS: Your up workflow to provision and deploy to Azure completed in 10 minutes 17 seconds.
      ```
1. В вашем портале Azure теперь будет создана группа ресурсов с именем вашего окружения:

      ![Infra](../../../../../translated_images/ru/02-provisioned-infra.46c706b14f56e0bf.webp)

1. **Теперь вы готовы проверить развернутую инфраструктуру и приложение**.

---

## 4. Проверка шаблона

1. Перейдите на страницу [Resource Groups](https://portal.azure.com/#browse/resourcegroups) в Azure Portal — войдите при появлении запроса
1. Нажмите на группу ресурсов вашего окружения — вы увидите страницу выше

      - нажмите на ресурс Azure Container Apps
      - нажмите на Application Url в разделе _Essentials_ (вверху справа)

1. Вы должны увидеть интерфейс размещенного приложения, подобный этому:

   ![App](../../../../../translated_images/ru/03-test-application.471910da12c3038e.webp)

1. Попробуйте задать пару [примерных вопросов](https://github.com/Azure-Samples/get-started-with-ai-agents/blob/main/docs/sample_questions.md)

      1. Спросите: ```What is the capital of France?``` 
      1. Спросите: ```What's the best tent under $200 for two people, and what features does it include?```

1. Вы должны получить ответы, похожие на показанные ниже. _Но как это работает?_ 

      ![App](../../../../../translated_images/ru/03-test-question.521c1e863cbaddb6.webp)

---

## 5. Проверка агента

Azure Container App разворачивает конечную точку, которая соединяется с AI агентом, созданным в проекте Microsoft Foundry для этого шаблона. Давайте посмотрим, что это значит.

1. Вернитесь на страницу _Overview_ вашей группы ресурсов в Azure Portal

1. Нажмите на ресурс `Microsoft Foundry` из списка

1. Вы должны увидеть это. Нажмите кнопку `Go to Microsoft Foundry Portal`. 
   ![Foundry](../../../../../translated_images/ru/04-view-foundry-project.fb94ca41803f28f3.webp)

1. Вы увидите страницу проекта Foundry для вашего AI приложения
   ![Project](../../../../../translated_images/ru/05-visit-foundry-portal.d734e98135892d7e.webp)

1. Нажмите на `Agents` — вы увидите стандартного агента, созданного в вашем проекте
   ![Agents](../../../../../translated_images/ru/06-visit-agents.bccb263f77b00a09.webp)

1. Выберите его — увидите детали агента. Обратите внимание на следующее:

      - Агент по умолчанию использует поиск по файлам (всегда)
      - В разделе `Knowledge` указано, что загружено 32 файла (для поиска по файлам)
      ![Agents](../../../../../translated_images/ru/07-view-agent-details.0e049f37f61eae62.webp)

1. Найдите в меню слева опцию `Data+indexes` и нажмите для подробностей. 

      - Вы увидите 32 файла данных, загруженных для создания знаний.
      - Они соответствуют 12 файлам клиентов и 20 файлам продуктов в директории `src/files` 
      ![Data](../../../../../translated_images/ru/08-visit-data-indexes.5a4cc1686fa0d19a.webp)

**Вы проверили работу агента!** 

1. Ответы агента основываются на знаниях из этих файлов. 
1. Теперь вы можете задавать вопросы, связанные с этими данными, и получать обоснованные ответы.
1. Например, `customer_info_10.json` описывает 3 покупки, сделанные "Amanda Perez"

Вернитесь во вкладку браузера с конечной точкой Container App и спросите: `What products does Amanda Perez own?`. Вы должны увидеть что-то похожее:

![Data](../../../../../translated_images/ru/09-ask-in-aca.4102297fc465a4d5.webp)

---

## 6. Площадка для агента

Давайте лучше разберемся в возможностях Microsoft Foundry, опробовав агента в Agents Playground. 

1. Вернитесь на страницу `Agents` в Microsoft Foundry — выберите стандартного агента
1. Нажмите опцию `Try in Playground` — вы увидите UI площадки, похожий на этот
1. Задайте тот же вопрос: `What products does Amanda Perez own?`

    ![Data](../../../../../translated_images/ru/09-ask-in-playground.a1b93794f78fa676.webp)

Вы получите тот же (или похожий) ответ — но также дополнительную информацию, которую можно использовать для понимания качества, затрат и производительности вашего агентного приложения. Например:

1. Обратите внимание, что в ответе указаны файлы данных, использованные для обоснования ответа
1. Наведите курсор на метки этих файлов — совпадают ли данные с вашим запросом и отображаемым ответом?

Вы также видите строку _stats_ под ответом. 

1. Наведите курсор на любой показатель — например, Safety. Вы увидите что-то похожее
1. Совпадает ли оценка с вашим ощущением уровня безопасности ответа?

      ![Data](../../../../../translated_images/ru/10-view-run-info-meter.6cdb89a0eea5531f.webp)

---

## 7. Встроенная наблюдаемость

Наблюдаемость — это инструментальный подход к тому, чтобы ваше приложение генерировало данные, которые можно использовать для понимания, отладки и оптимизации работы. Чтобы ознакомиться с этим:

1. Нажмите кнопку `View Run Info` — вы увидите этот экран. Это пример [трассировки агента](https://learn.microsoft.com/en-us/azure/ai-foundry/how-to/develop/trace-agents-sdk#view-trace-results-in-the-azure-ai-foundry-agents-playground) в действии. _Также этот экран можно открыть, нажав Thread Logs в главном меню_.

   - Ознакомьтесь с шагами запуска и инструментами, задействованными агентом
   - Посмотрите общее количество токенов (по сравнению с использованием выходных токенов) для ответа
   - Поймите задержку и где тратится время при выполнении

      ![Agent](../../../../../translated_images/ru/10-view-run-info.b20ebd75fef6a1cc.webp)

1. Нажмите вкладку `Metadata`, чтобы увидеть дополнительные атрибуты запуска, которые могут быть полезны для последующей отладки.   

      ![Agent](../../../../../translated_images/ru/11-view-run-info-metadata.7966986122c7c2df.webp)


1. Нажмите вкладку `Evaluations` для просмотра автооценок, сделанных по ответу агента. Включает проверки безопасности (например, Self-harm) и специфические для агента оценки (например, разрешение намерений, выполнение задач).

      ![Agent](../../../../../translated_images/ru/12-view-run-info-evaluations.ef25e4577d70efeb.webp)

1. И наконец, нажмите вкладку `Monitoring` в боковом меню.

      - Выберите вкладку `Resource usage` на странице — и посмотрите метрики.
      - Отслеживайте использование приложения в терминах затрат (токены) и нагрузки (запросы).
      - Отслеживайте задержку приложения до первого байта (обработка входных данных) и до последнего байта (вывод).

      ![Agent](../../../../../translated_images/ru/13-monitoring-resources.5148015f7311807f.webp)

---

## 8. Переменные окружения

До сих пор мы по шагам развертывали приложение через браузер — и проверили, что инфраструктура создана и приложение работает. Но чтобы работать с приложением _сначала через код_, нам нужно настроить локальное окружение разработчика с соответствующими переменными, используемыми для работы с этими ресурсами. `azd` делает это просто.

1. Azure Developer CLI [использует переменные окружения](https://learn.microsoft.com/en-us/azure/developer/azure-developer-cli/manage-environment-variables?tabs=bash) для хранения и управления настройками конфигурации при развертывании приложений.

1. Переменные окружения хранятся в `.azure/<env-name>/.env` — это определяет их для окружения `env-name`, используемого при развертывании, и помогает изолировать окружения для разных целей деплоя в одном репозитории.

1. Переменные окружения автоматически загружаются командой `azd` при выполнении определенной команды (например, `azd up`). Обратите внимание, что `azd` не читает автоматически системные (OS-level) переменные окружения (например, установленные в оболочке) — вместо этого используйте `azd set env` и `azd get env` для передачи информации через скрипты.


Давайте попробуем несколько команд:

1. Получите все переменные окружения, установленные для `azd` в этом окружении:

      ```bash title="" linenums="0"
      azd env get-values
      ```
      
      Вы увидите что-то вроде:

      ```bash title="" linenums="0"
      AZURE_AI_AGENT_DEPLOYMENT_NAME="gpt-4o-mini"
      AZURE_AI_AGENT_NAME="agent-template-assistant"
      AZURE_AI_EMBED_DEPLOYMENT_NAME="text-embedding-3-small"
      AZURE_AI_EMBED_DIMENSIONS=100
      ...
      ```

1. Получите конкретное значение — например, я хочу узнать, была ли установлена переменная `AZURE_AI_AGENT_MODEL_NAME`

      ```bash title="" linenums="0"
      azd env get-value AZURE_AI_AGENT_MODEL_NAME 
      ```
      
      Вы увидите что-то вроде этого — по умолчанию оно не установлено!

      ```bash title="" linenums="0"
      ERROR: key 'AZURE_AI_AGENT_MODEL_NAME' not found in the environment values
      ```

1. Установите новую переменную окружения для `azd`. Здесь мы обновляем имя модели агента. _Важно: все изменения сразу же отразятся в файле `.azure/<env-name>/.env`._

      ```bash title="" linenums="0"
      azd env set AZURE_AI_AGENT_MODEL_NAME gpt-4.1
      azd env set AZURE_AI_AGENT_MODEL_VERSION 2025-04-14
      azd env set AZURE_AI_AGENT_DEPLOYMENT_CAPACITY 150
      ```

      Теперь значение должно быть установлено:

      ```bash title="" linenums="0"
      azd env get-value AZURE_AI_AGENT_MODEL_NAME 
      ```

1. Обратите внимание, что некоторые ресурсы являются постоянными (например, развертывания моделей) и потребуют не только `azd up`, чтобы заставить их повторно развернуться. Давайте попробуем удалить исходное развертывание и развернуть снова с измененными переменными окружения.

1. **Обновление** Если вы ранее развертывали инфраструктуру с помощью шаблона azd, вы можете _обновить_ состояние локальных переменных среды на основе текущего состояния вашего Azure-развертывания с помощью этой команды:

      ```bash title="" linenums="0"
      azd env refresh
      ```

      Это мощный способ _синхронизации_ переменных окружения между двумя или более локальными средами разработки (например, команда с несколькими разработчиками) — позволяющий развернутой инфраструктуре выступать в роли источника истины для состояния переменных окружения. Члены команды просто _обновляют_ переменные, чтобы снова синхронизироваться.

---

## 9. Поздравляем 🏆

Вы только что выполнили полный рабочий процесс, в котором вы:

- [X] Выбрали шаблон AZD, который хотите использовать
- [X] Запустили шаблон с помощью GitHub Codespaces
- [X] Развернули шаблон и подтвердили, что он работает

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Отказ от ответственности**:  
Этот документ был переведен с помощью автоматического сервиса перевода [Co-op Translator](https://github.com/Azure/co-op-translator). Несмотря на наши усилия по обеспечению точности, имейте в виду, что автоматический перевод может содержать ошибки или неточности. Оригинальный документ на родном языке следует рассматривать в качестве авторитетного источника. Для критически важной информации рекомендуется воспользоваться услугами профессионального переводчика. Мы не несем ответственности за любые недоразумения или неправильные толкования, возникшие в результате использования данного перевода.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->