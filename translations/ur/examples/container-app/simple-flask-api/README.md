# سادہ فلاسک API - کنٹینر ایپ کی مثال

**تعلمی راستہ:** ابتدائی ⭐ | **وقت:** 25-35 منٹ | **لاگت:** $0-15/ماہ

ایک مکمل، کام کرنے والا Python Flask REST API جو Azure Developer CLI (azd) استعمال کرتے ہوئے Azure Container Apps پر تعینات کیا گیا ہے۔ اس مثال میں کنٹینر کی تعیناتی، خودکار اسکیلنگ، اور مانیٹرنگ کی بنیادیں دکھائی گئی ہیں۔

## 🎯 آپ کیا سیکھیں گے

- Python ایپلیکیشن کو Azure پر کنٹینرز کی صورت میں تعینات کرنا
- اسکیل ٹو زیرو کے ساتھ خودکار اسکیلنگ کی ترتیب دینا
- ہیلتھ پروبز اور ریڈینیس چیکس کو نافذ کرنا
- ایپلیکیشن لاگز اور میٹرکس کی نگرانی کرنا
- تیز تعیناتی کے لیے Azure Developer CLI کا استعمال

## 📦 اس میں کیا شامل ہے

✅ **فلاسک ایپلیکیشن** - CRUD آپریشنز کے ساتھ مکمل REST API (`src/app.py`)  
✅ **ڈوکر فائل** - پروڈکشن کے لیے تیار کنٹینر کی ترتیب  
✅ **بِیسپ انفراسٹرکچر** - کنٹینر ایپس کا ماحول اور API کی تعیناتی  
✅ **AZD کنفیگریشن** - ایک کمانڈ میں تعیناتی کی ترتیب  
✅ **ہیلتھ پروبز** - لیونیس اور ریڈینیس چیکس ترتیب دی گئی ہیں  
✅ **خودکار اسکیلنگ** - HTTP لوڈ کی بنیاد پر 0-10 ریپلیکاز  

## فن تعمیر

```
┌─────────────────────────────────────────┐
│   Azure Container Apps Environment      │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │   Flask API Container             │ │
│  │   - Health endpoints              │ │
│  │   - REST API                      │ │
│  │   - Auto-scaling (0-10 replicas)  │ │
│  └───────────────────────────────────┘ │
│                                         │
│  Application Insights ────────────────┐ │
└────────────────────────────────────────┘
```

## ضروریات

### درکار
- **Azure Developer CLI (azd)** - [انسٹال گائیڈ](https://learn.microsoft.com/azure/developer/azure-developer-cli/install-azd)
- **Azure سبسکرپشن** - [مفت اکاؤنٹ](https://azure.microsoft.com/free/)
- **ڈوکر ڈیسک ٹاپ** - [ڈوکر انسٹال کریں](https://www.docker.com/products/docker-desktop/) (مقامی جانچ کے لیے)

### ضروریات کی تصدیق کریں

```bash
# ورژن azd چیک کریں (1.5.0 یا اس سے زیادہ درکار ہے)
azd version

# Azure لاگ ان کی تصدیق کریں
azd auth login

# Docker چیک کریں (اختیاری، مقامی ٹیسٹنگ کے لیے)
docker --version
```

## ⏱️ تعیناتی کا وقت

| مرحلہ | دورانیہ | کیا ہوتا ہے |
|-------|----------|--------------||
| ماحول کی تیاری | 30 سیکنڈ | azd ماحول بنائیں |
| کنٹینر کی تعمیر | 2-3 منٹ | Flask ایپ کی ڈوکر تعمیر |
| انفراسٹرکچر کی فراہمی | 3-5 منٹ | کنٹینر ایپس، رجسٹری، مانیٹرنگ بنائیں |
| ایپلیکیشن کی تعیناتی | 2-3 منٹ | امیج کو پش کریں اور کنٹینر ایپس پر تعینات کریں |
| **کل** | **8-12 منٹ** | مکمل تعیناتی تیار |

## تیز آغاز

```bash
# مثال پر جائیں
cd examples/container-app/simple-flask-api

# ماحول کا آغاز کریں (منفرد نام منتخب کریں)
azd env new myflaskapi

# سب کچھ تعینات کریں (انفراسٹرکچر + ایپلیکیشن)
azd up
# آپ سے پوچھا جائے گا:
# 1. ایزور سب سکریپشن منتخب کریں
# 2. مقام منتخب کریں (مثلاً، eastus2)
# 3. تعیناتی کے لیے 8-12 منٹ انتظار کریں

# اپنا API اینڈپوائنٹ حاصل کریں
azd env get-values

# API کی جانچ کریں
curl $(azd env get-value API_ENDPOINT)/health
```

**متوقع نتیجہ:**
```json
{
  "status": "healthy",
  "timestamp": "2025-11-19T10:30:00Z",
  "service": "simple-flask-api",
  "version": "1.0.0"
}
```

## ✅ تعیناتی کی تصدیق کریں

### مرحلہ 1: تعیناتی کی حالت چیک کریں

```bash
# تعین شدہ خدمات دیکھیں
azd show

# متوقع نتیجہ دکھاتا ہے:
# - سروس: api
# - اینڈ پوائنٹ: https://ca-api-[env].xxx.azurecontainerapps.io
# - حیثیت: چل رہا ہے
```

### مرحلہ 2: API اینڈ پوائنٹس کی جانچ کریں

```bash
# ای پی آئی اینڈ پوائنٹ حاصل کریں
API_URL=$(azd env get-value API_ENDPOINT)

# صحت کا معائنہ کریں
curl $API_URL/health

# روٹ اینڈ پوائنٹ کا معائنہ کریں
curl $API_URL/

# ایک آئٹم بنائیں
curl -X POST $API_URL/api/items \
  -H "Content-Type: application/json" \
  -d '{"name": "Test Item", "description": "My first item"}'

# تمام آئٹمز حاصل کریں
curl $API_URL/api/items
```

**کامیابی کے معیار:**
- ✅ ہیلتھ اینڈ پوائنٹ HTTP 200 واپس کرتا ہے
- ✅ روٹ اینڈ پوائنٹ API معلومات دکھاتا ہے
- ✅ POST نئی چیز بناتا ہے اور HTTP 201 واپس کرتا ہے
- ✅ GET بنائی گئی اشیا واپس کرتا ہے

### مرحلہ 3: لاگز دیکھیں

```bash
# زید مانیٹر کا استعمال کرتے ہوئے لائیو لاگز سٹریم کریں
azd monitor --logs

# یا Azure CLI استعمال کریں:
az containerapp logs show --name api --resource-group $RG_NAME --follow

# آپ کو دکھائی دے گا:
# - گنیکورن کے اسٹارٹ اپ پیغامات
# - HTTP ریکویسٹ لاگز
# - ایپلیکیشن کی معلومات کے لاگز
```

## پراجیکٹ ساخت

```
simple-flask-api/
├── azure.yaml              # AZD configuration
├── infra/
│   ├── main.bicep         # Main infrastructure
│   ├── main.parameters.json
│   └── app/
│       ├── container-env.bicep
│       └── api.bicep
└── src/
    ├── app.py             # Flask application
    ├── requirements.txt
    └── Dockerfile
```

## API اینڈ پوائنٹس

| اینڈ پوائنٹ | طریقہ | وضاحت |
|----------|--------|-------------|
| `/health` | GET | صحت کی جانچ |
| `/api/items` | GET | تمام اشیا کی فہرست |
| `/api/items` | POST | نئی چیز بنائیں |
| `/api/items/{id}` | GET | مخصوص چیز حاصل کریں |
| `/api/items/{id}` | PUT | چیز کو اپ ڈیٹ کریں |
| `/api/items/{id}` | DELETE | چیز کو حذف کریں |

## ترتیب

### ماحول کے متغیرات

```bash
# حسبِ ضرورت ترتیب سیٹ کریں
azd env set PORT 8000
azd env set LOG_LEVEL info
azd env set MAX_REPLICAS 20
```

### اسکیلنگ کی ترتیب

API خودکار طور پر HTTP ٹریفک کی بنیاد پر اسکیل کرتی ہے:
- **کم از کم ریپلیکاز**: 0 (خالی ہونے پر صفر تک اسکیل ہوتا ہے)
- **زیادہ سے زیادہ ریپلیکاز**: 10
- **ریپلیکا فی بیک وقت درخواستیں**: 50

## ترقیاتی مراحل

### مقامی طور پر چلائیں

```bash
# انحصارات انسٹال کریں
cd src
pip install -r requirements.txt

# ایپ چلائیں
python app.py

# مقامی طور پر جانچ کریں
curl http://localhost:8000/health
```

### کنٹینر بنائیں اور ٹیسٹ کریں

```bash
# ڈاکر امیج بنائیں
docker build -t flask-api:local ./src

# کنٹینر کو مقامی طور پر چلائیں
docker run -p 8000:8000 flask-api:local

# کنٹینر کا ٹیسٹ کریں
curl http://localhost:8000/health
```

## تعیناتی

### مکمل تعیناتی

```bash
# انفراسٹرکچر اور ایپلیکیشن کو تعینات کریں
azd up
```

### صرف کوڈ کی تعیناتی

```bash
# صرف ایپلیکیشن کوڈ تعینات کریں (انفراسٹرکچر بغیر تبدیلی کے)
azd deploy api
```

### ترتیب کو اپ ڈیٹ کریں

```bash
# ماحول کے متغیرات کو اپڈیٹ کریں
azd env set API_KEY "new-api-key"

# نئی ترتیب کے ساتھ دوبارہ تعینات کریں
azd deploy api
```

## مانیٹرنگ

### لاگز دیکھیں

```bash
# لاگز براہ راست azd monitor کے ذریعے سٹریم کریں
azd monitor --logs

# یا Container Apps کے لیے Azure CLI استعمال کریں:
az containerapp logs show --name api --resource-group $RG_NAME --follow

# آخری 100 لائنز دیکھیں
az containerapp logs show --name api --resource-group $RG_NAME --tail 100
```

### میٹرکس کی نگرانی کریں

```bash
# ایزور مانیٹر ڈیش بورڈ کھولیں
azd monitor --overview

# مخصوص میٹرکس دیکھیں
az monitor metrics list \
  --resource $(azd show --output json | jq -r '.services.api.resourceId') \
  --metric "Requests,ResponseTime"
```

## جانچ

### ہیلتھ چیک

```bash
curl $(azd show --output json | jq -r '.services.api.endpoint')/health
```

متوقع جواب:
```json
{
  "status": "healthy",
  "timestamp": "2025-11-19T10:30:00Z"
}
```

### چیز بنائیں

```bash
curl -X POST $(azd show --output json | jq -r '.services.api.endpoint')/api/items \
  -H "Content-Type: application/json" \
  -d '{"name": "Test Item", "description": "A test item"}'
```

### تمام اشیا حاصل کریں

```bash
curl $(azd show --output json | jq -r '.services.api.endpoint')/api/items
```

## لاگت کی بچت

یہ تعیناتی اسکیل ٹو زیرو استعمال کرتی ہے، اس لیے آپ صرف اس وقت ادائیگی کرتے ہیں جب API درخواستیں پروسیس کر رہا ہو:

- **خالی لاگت**: تقریباً $0/ماہ (زیرو تک اسکیل ہوتا ہے)
- **فعال لاگت**: ہر ریپلیکا کے لیے تقریباً $0.000024/سیکنڈ
- **متوقع ماہانہ لاگت** (ہلکی استعمال): $5-15

### مزید لاگت کم کریں

```bash
# ڈیولپمنٹ کے لیے زیادہ سے زیادہ ریپلیکاز کو کم کریں
azd env set MAX_REPLICAS 3

# چھوٹا بے کار ٹائم آؤٹ استعمال کریں
azd env set SCALE_TO_ZERO_TIMEOUT 300  # 5 منٹ
```

## مسائل کا حل

### کنٹینر شروع نہیں ہوتا

```bash
# Azure CLI کا استعمال کرتے ہوئے کنٹینر کے لاگز چیک کریں
az containerapp logs show --name api --resource-group $RG_NAME --tail 100

# Docker امیج کی مقامی طور پر تعمیر کی تصدیق کریں
docker build -t test ./src
```

### API قابل رسائی نہیں ہے

```bash
# تصدیق کریں کہ انگریس بیرونی ہے
az containerapp show --name api --resource-group rg-simple-flask-api \
  --query properties.configuration.ingress.external
```

### جواب دینے میں دیر

```bash
# سی پی یو/میموری کے استعمال کو چیک کریں
az monitor metrics list \
  --resource $(azd show --output json | jq -r '.services.api.resourceId') \
  --metric "CPUPercentage,MemoryPercentage"

# ضرورت پڑنے پر وسائل کو بڑھائیں
az containerapp update --name api --resource-group rg-simple-flask-api \
  --cpu 1.0 --memory 2Gi
```

## صفائی

```bash
# تمام وسائل کو حذف کریں
azd down --force --purge
```

## اگلے اقدامات

### اس مثال کو بڑھائیں

1. **ڈیٹابیس شامل کریں** - Azure Cosmos DB یا SQL Database کا انضمام
   ```bash
   # infra/main.bicep میں Cosmos DB ماڈیول شامل کریں
   # app.py کو ڈیٹا بیس کنکشن کے ساتھ اپ ڈیٹ کریں
   ```

2. **تصدیق شامل کریں** - Azure AD یا API کلیدیں نافذ کریں
   ```python
   # app.py میں توثیقی مڈل ویئر شامل کریں
   from functools import wraps
   ```

3. **CI/CD سیٹ اپ کریں** - GitHub Actions ورک فلو
   ```yaml
   # Create .github/workflows/deploy.yml
   name: Deploy to Azure
   on: [push]
   ```

4. **منیجڈ آئیڈینٹی شامل کریں** - Azure سروسز تک محفوظ رسائی
   ```bicep
   # Update infra/app/api.bicep
   identity: { type: 'SystemAssigned' }
   ```

### متعلقہ مثالیں

- **[ڈیٹابیس ایپ](../../../../../examples/database-app)** - SQL Database کے ساتھ مکمل مثال  
- **[مائیکرو سروسز](../../../../../examples/container-app/microservices)** - کثیر خدمت فن تعمیر  
- **[کنٹینر ایپس ماسٹر گائیڈ](../README.md)** - تمام کنٹینر پیٹرنز

### تعلیمی ذرائع

- 📚 [ابتدائیوں کے لیے AZD کورس](../../../README.md) - مرکزی کورس ہوم  
- 📚 [کنٹینر ایپس پیٹرنز](../README.md) - مزید تعیناتی کے نمونے  
- 📚 [AZD ٹیمپلیٹس گیلری](https://azure.github.io/awesome-azd/) - کمیونٹی ٹیمپلیٹس

## اضافی ذرائع

### دستاویزی مواد
- **[فلاسک دستاویزات](https://flask.palletsprojects.com/)** - فلاسک فریم ورک گائیڈ  
- **[Azure Container Apps](https://learn.microsoft.com/azure/container-apps/)** - Azure کی سرکاری دستاویزات  
- **[Azure Developer CLI](https://learn.microsoft.com/azure/developer/azure-developer-cli/)** - azd کمانڈ ریفرنس

### ٹیوٹوریل
- **[کنٹینر ایپس کوئیک اسٹارٹ](https://learn.microsoft.com/azure/container-apps/quickstart-portal)** - اپنی پہلی ایپ تعینات کریں  
- **[Azure پر Python](https://learn.microsoft.com/azure/developer/python/)** - Python ترقیاتی گائیڈ  
- **[بِیسپ لینگویج](https://learn.microsoft.com/azure/azure-resource-manager/bicep/)** - کوڈ کے طور پر انفراسٹرکچر

### ٹولز
- **[Azure پورٹل](https://portal.azure.com)** - وسائل کا بصری انتظام  
- **[VS کوڈ Azure ایکسٹینشن](https://marketplace.visualstudio.com/items?itemName=ms-azuretools.vscode-azurecontainerapps)** - IDE انضمام

---

**🎉 مبارک ہو!** آپ نے Azure Container Apps پر خودکار اسکیلنگ اور مانیٹرنگ کے ساتھ ایک پروڈکشن کے قابل Flask API تعینات کر دیا ہے۔

**سوالات؟** [مسئلہ کھولیں](https://github.com/microsoft/AZD-for-beginners/issues) یا [FAQ](../../../resources/faq.md) دیکھیں۔

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**免责声明**:
یہ دستاویز AI ترجمہ سروس [Co-op Translator](https://github.com/Azure/co-op-translator) کے ذریعے ترجمہ کی گئی ہے۔ اگرچہ ہم درستگی کے لیے کوشاں ہیں، براہ کرم نوٹ کریں کہ خودکار ترجموں میں غلطیاں یا غیر درستیاں ہو سکتی ہیں۔ اصل دستاویز اپنی مادری زبان میں معتبر ذریعہ سمجھا جانا چاہیے۔ اہم معلومات کے لیے پیشہ ورانہ انسانی ترجمہ کی سفارش کی جاتی ہے۔ اس ترجمے کے استعمال سے ہونے والی کسی بھی غلط فہمی یا غلط تعبیر کی ذمہ داری ہم پر نہیں ہوگی۔
<!-- CO-OP TRANSLATOR DISCLAIMER END -->