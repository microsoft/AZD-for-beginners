# عام مسائل اور حل

**باب نیویگیشن:**
- **📚 کورس ہوم**: [AZD برائے ابتدائی افراد](../../README.md)
- **📖 موجودہ باب**: باب 7 - مسائل کا ازالہ اور ڈیبگنگ
- **⬅️ پچھلا باب**: [باب 6: پری-ڈیپلائمنٹ چیکس](../chapter-06-pre-deployment/preflight-checks.md)
- **➡️ اگلا**: [ڈیبگنگ گائیڈ](debugging.md)
- **🚀 اگلا باب**: [باب 8: پروڈکشن & انٹرپرائز پیٹرنز](../chapter-08-production/production-ai-practices.md)

## تعارف

یہ جامع ٹربل شوٹنگ گائیڈ Azure Developer CLI استعمال کرتے وقت سامنے آنے والے سب سے عام مسائل کا احاطہ کرتی ہے۔ سیکھیں کہ کس طرح تصدیق، تعیناتی، انفراسٹرکچر پروویژننگ، اور ایپلیکیشن کنفیگریشن کے عام مسائل کی تشخیص، ٹربل شوٹنگ، اور حل کریں۔ ہر مسئلے میں تفصیلی علامات، جڑ کے اسباب، اور مرحلہ بہ مرحلہ حل کے طریقۂ کار شامل ہیں۔

## سیکھنے کے اہداف

مکمل کرنے کے بعد آپ:
- Azure Developer CLI کے مسائل کے لیے تشخیصی تکنیکس میں ماہر بن جائیں گے
- عام تصدیق اور اجازت کے مسائل اور ان کے حل کو سمجھیں گے
- ڈیپلائمنٹ کی ناکامیوں، انفراسٹرکچر پروویژننگ غلطیوں، اور کنفیگریشن مسائل کو حل کریں گے
- پروایکٹو مانیٹرنگ اور ڈیبگنگ حکمتِ عملیاں نافذ کریں گے
- پیچیدہ مسائل کے لیے منظم ٹربل شوٹنگ طریقۂ کار اپنائیں گے
- مستقبل کے مسائل کی روک تھام کے لیے مناسب لاگنگ اور مانیٹرنگ ترتیب دیں گے

## سیکھنے کے نتائج

مکمل کرنے کے بعد آپ قابل ہوں گے:
- بلٹ اِن تشخیصی ٹولز استعمال کرتے ہوئے Azure Developer CLI کے مسائل کی تشخیص کریں
- تصدیق، سبسکرپشن، اور اجازت سے متعلق مسائل کو خود مختاری سے حل کریں
- ڈیپلائمنٹ کی ناکامیوں اور انفراسٹرکچر پروویژننگ کی غلطیوں کو مؤثر انداز میں ٹربل شوٹ کریں
- ایپلیکیشن کنفیگریشن کے مسائل اور ماحول سے متعلق مخصوص مسائل کو ڈیبگ کریں
- ممکنہ مسائل کی پروایکٹو شناخت کے لیے مانیٹرنگ اور الرٹنگ نافذ کریں
- لاگنگ، ڈیبگنگ، اور مسئلہ حل کرنے کے ورک فلو کے بہترین عملی طریقے اپنائیں

## فوری تشخیصی اقدامات

مخصوص مسائل میں غوطہ لگانے سے پہلے، تشخیصی معلومات جمع کرنے کے لیے یہ کمانڈز چلائیں:

```bash
# azd کا ورژن اور حالت چیک کریں
azd version
azd config list

# Azure کی توثیق کی جانچ کریں
az account show
az account list

# موجودہ ماحول چیک کریں
azd env show
azd env get-values

# ڈیبگ لاگنگ فعال کریں
export AZD_DEBUG=true
azd <command> --debug
```

## تصدیقی مسائل

### مسئلہ: "ایکسس ٹوکن حاصل کرنے میں ناکامی"
**علامات:**
- `azd up` تصدیق کی خرابیوں کے ساتھ ناکام ہوتا ہے
- کمانڈز "unauthorized" یا "access denied" واپس کرتی ہیں

**حل:**
```bash
# 1. Azure CLI کے ساتھ دوبارہ لاگ ان کریں
az login
az account show

# 2. کیش شدہ اسناد کو صاف کریں
az account clear
az login

# 3. ڈیوائس کوڈ فلو استعمال کریں (ہیڈ لیس سسٹمز کے لیے)
az login --use-device-code

# 4. مخصوص سبسکرپشن مقرر کریں
az account set --subscription "your-subscription-id"
azd config set defaults.subscription "your-subscription-id"
```

### مسئلہ: "ناکافی اختیارات" ڈیپلائمنٹ کے دوران
**علامات:**
- ڈیپلائمنٹ اجازت کی خرابیوں کے ساتھ ناکام ہوجاتی ہے
- مخصوص Azure ریسورسز تخلیق نہیں ہو سکتیں

**حل:**
```bash
# 1. اپنے Azure رول تفویضات چیک کریں
az role assignment list --assignee $(az account show --query user.name -o tsv)

# 2. یقینی بنائیں کہ آپ کے پاس درکار رولز موجود ہیں
# - Contributor (وسائل کی تخلیق کے لیے)
# - User Access Administrator (رول تفویضات کے لیے)

# 3. مناسب اجازتوں کے لیے اپنے Azure ایڈمنسٹریٹر سے رابطہ کریں
```

### مسئلہ: ملٹی-ٹیننٹ تصدیق کے مسائل
**حل:**
```bash
# 1. مخصوص ٹیننٹ کے ساتھ لاگ ان کریں
az login --tenant "your-tenant-id"

# 2. کنفیگریشن میں ٹیننٹ سیٹ کریں
azd config set auth.tenantId "your-tenant-id"

# 3. اگر ٹیننٹس تبدیل کر رہے ہوں تو ٹیننٹ کی کیش صاف کریں
az account clear
```

## 🏗️ انفراسٹرکچر پروویژننگ کی خرابییں

### مسئلہ: ریسورس ناموں کا تنازع
**علامات:**
- "The resource name already exists" جیسی غلطیاں
- ریسورس تخلیق کے دوران ڈیپلائمنٹ ناکام ہوجاتا ہے

**حل:**
```bash
# 1. ٹوکنز کے ساتھ منفرد وسائل کے نام استعمال کریں
# آپ کے Bicep ٹیمپلیٹ میں:
var resourceToken = toLower(uniqueString(subscription().id, environmentName, location))
name: '${applicationName}-${resourceToken}'

# 2. ماحول کا نام تبدیل کریں
azd env new my-app-dev-$(whoami)-$(date +%s)

# 3. موجودہ وسائل کی صفائی کریں
azd down --force --purge
```

### مسئلہ: مقام/ریجن دستیاب نہیں ہے
**علامات:**
- "The location 'xyz' is not available for resource type"
- بعض SKUs منتخب کردہ ریجن میں دستیاب نہیں ہیں

**حل:**
```bash
# 1. وسائل کی اقسام کے لیے دستیاب مقامات چیک کریں
az provider show --namespace Microsoft.Web --query "resourceTypes[?resourceType=='sites'].locations" -o table

# 2. عام طور پر دستیاب خطوں کا استعمال کریں
azd config set defaults.location eastus2
# یا
azd env set AZURE_LOCATION eastus2

# 3. خطے کے مطابق سروس کی دستیابی چیک کریں
# ملاحظہ کریں: https://azure.microsoft.com/global-infrastructure/services/
```

### مسئلہ: کوٹا سے تجاوز کی غلطیاں
**علامات:**
- "Quota exceeded for resource type"
- "Maximum number of resources reached"

**حل:**
```bash
# 1. موجودہ کوٹا کا استعمال چیک کریں
az vm list-usage --location eastus2 -o table

# 2. Azure پورٹل کے ذریعے کوٹا بڑھانے کی درخواست کریں
# پر جائیں: سبسکرپشنز > استعمال + کوٹے

# 3. ڈویلپمنٹ کے لیے چھوٹے SKUs استعمال کریں
# main.parameters.json میں:
{
  "appServiceSku": {
    "value": "B1"  // Instead of P1v3
  }
}

# 4. غیر استعمال شدہ وسائل کو صاف کریں
az resource list --query "[?contains(name, 'unused')]" -o table
```

### مسئلہ: Bicep ٹیمپلیٹ کی خرابییں
**علامات:**
- ٹیمپلیٹ کی ویلیڈیشن فیل ہونا
- Bicep فائلوں میں سنٹیکس ایررز

**حل:**
```bash
# 1. Bicep کے نحو کی تصدیق کریں
az bicep build --file infra/main.bicep

# 2. Bicep کے لِنٹر کا استعمال کریں
az bicep lint --file infra/main.bicep

# 3. پیرامیٹر فائل کے نحو کی تصدیق کریں
cat infra/main.parameters.json | jq '.'

# 4. تعیناتی تبدیلیوں کا پیش نظارہ کریں
azd provision --preview
```

## 🚀 ڈیپلائمنٹ کی ناکامیاں

### مسئلہ: بلڈ کی ناکامیاں
**علامات:**
- ڈیپلائمنٹ کے دوران ایپلیکیشن بلڈ نہیں ہوتی
- پیکیج انسٹالیشن ایررز

**حل:**
```bash
# 1. ڈیبگ فلیگ کے ساتھ بلڈ آؤٹ پٹ چیک کریں
azd deploy --service web --debug

# 2. تعین شدہ سروس کی حیثیت دیکھیں
azd show

# 3. مقامی طور پر بلڈ ٹیسٹ کریں
cd src/web
npm install
npm run build

# 3. Node.js/Python کے ورژن کی مطابقت چیک کریں
node --version  # azure.yaml کی ترتیبات سے میل کھانی چاہیے
python --version

# 4. بلڈ کیش صاف کریں
rm -rf node_modules package-lock.json
npm install

# 5. اگر کنٹینرز استعمال کر رہے ہوں تو Dockerfile چیک کریں
docker build -t test-image .
docker run --rm test-image
```

### مسئلہ: کنٹینر ڈیپلائمنٹ کی ناکامیاں
**علامات:**
- کنٹینر ایپس شروع نہیں ہوتیں
- امیج پل ایررز

**حل:**
```bash
# 1. مقامی طور پر Docker بلڈ کا ٹیسٹ کریں
docker build -t my-app:latest .
docker run --rm -p 3000:3000 my-app:latest

# 2. Azure CLI استعمال کرتے ہوئے کنٹینر کے لاگز چیک کریں
az containerapp logs show --name my-app --resource-group my-rg --follow

# 3. azd کے ذریعے ایپلیکیشن کی نگرانی کریں
azd monitor --logs

# 3. کنٹینر رجسٹری تک رسائی کی تصدیق کریں
az acr login --name myregistry

# 4. کنٹینر ایپ کی کنفیگریشن چیک کریں
az containerapp show --name my-app --resource-group my-rg
```

### مسئلہ: ڈیٹا بیس کنکشن کی ناکامیاں
**علامات:**
- ایپلیکیشن ڈیٹا بیس سے کنیکٹ نہیں کر پاتی
- کنکشن ٹائم آؤٹ ایررز

**حل:**
```bash
# 1. ڈیٹا بیس کے فائر وال قواعد چیک کریں
az postgres flexible-server firewall-rule list --name mydb --resource-group myrg

# 2. ایپلیکیشن سے کنکشن کی جانچ کریں
# اپنی ایپ میں عارضی طور پر شامل کریں:
curl -v telnet://mydb.postgres.database.azure.com:5432

# 3. کنکشن اسٹرنگ کے فارمیٹ کی تصدیق کریں
azd env get-values | grep DATABASE

# 4. ڈیٹا بیس سرور کی حالت چیک کریں
az postgres flexible-server show --name mydb --resource-group myrg --query state
```

## 🔧 کنفیگریشن کے مسائل

### مسئلہ: ماحولاتی ویری ایبلز کام نہیں کر رہیں
**علامات:**
- ایپ کنفیگریشن کے اقدار نہیں پڑھ پا رہی
- ماحولاتی ویری ایبلز خالی دکھائی دیتی ہیں

**حل:**
```bash
# 1. اس بات کی تصدیق کریں کہ ماحولیاتی متغیرات سیٹ ہیں
azd env get-values
azd env get DATABASE_URL

# 2. azure.yaml میں متغیرات کے نام چیک کریں
cat azure.yaml | grep -A 5 env:

# 3. ایپلیکیشن کو دوبارہ شروع کریں
azd deploy --service web

# 4. ایپ سروس کی ترتیب چیک کریں
az webapp config appsettings list --name myapp --resource-group myrg
```

### مسئلہ: SSL/TLS سرٹیفیکیٹ کے مسائل
**علامات:**
- HTTPS کام نہیں کر رہا
- سرٹیفیکیٹ کی ویلیڈیشن ایررز

**حل:**
```bash
# 1. SSL سرٹیفکیٹ کی حیثیت چیک کریں
az webapp config ssl list --resource-group myrg

# 2. صرف HTTPS فعال کریں
az webapp update --name myapp --resource-group myrg --https-only true

# 3. کسٹم ڈومین شامل کریں (اگر ضرورت ہو)
az webapp config hostname add --webapp-name myapp --resource-group myrg --hostname mydomain.com
```

### مسئلہ: CORS کنفیگریشن کے مسائل
**علامات:**
- فرنٹ اینڈ API کال نہیں کر پا رہا
- کراس-اورجن ریکویسٹ بلاک ہو رہی ہے

**حل:**
```bash
# 1. App Service کے لیے CORS ترتیب دیں
az webapp cors add --name myapi --resource-group myrg --allowed-origins https://myapp.azurewebsites.net

# 2. CORS کو سنبھالنے کے لیے API کو اپ ڈیٹ کریں
# Express.js میں:
app.use(cors({
  origin: process.env.FRONTEND_URL,
  credentials: true
}));

# 3. چیک کریں کہ آیا یہ درست یو آر ایلز پر چل رہا ہے
azd show
```

## 🌍 ماحول مینجمنٹ کے مسائل

### مسئلہ: ماحول سوئچنگ کے مسائل
**علامات:**
- غلط ماحول استعمال ہو رہا ہے
- کنفیگریشن درست طریقے سے سوئچ نہیں ہو رہی

**حل:**
```bash
# 1. تمام ماحول کی فہرست بنائیں
azd env list

# 2. ماحول کو واضح طور پر منتخب کریں
azd env select production

# 3. موجودہ ماحول کی تصدیق کریں
azd env show

# 4. اگر خراب ہو تو نیا ماحول بنائیں
azd env new production-new
azd env select production-new
```

### مسئلہ: ماحول کا کرپشن ہونا
**علامات:**
- ماحول میں غیر درست حالت دکھائی دیتی ہے
- وسائل کنفیگریشن سے میل نہیں کھاتے

**حل:**
```bash
# 1. ماحول کی حالت کو تازہ کریں
azd env refresh

# 2. ماحول کی ترتیب کو ری سیٹ کریں
azd env new production-reset
# ضروری ماحولیاتی متغیرات کو کاپی کریں
azd env set DATABASE_URL "your-value"

# 3. موجودہ وسائل کو درآمد کریں (اگر ممکن ہو)
# دستی طور پر .azure/production/config.json کو وسائل کے شناختی نمبروں کے ساتھ اپ ڈیٹ کریں
```

## 🔍 کارکردگی کے مسائل

### مسئلہ: ڈیپلائمنٹ کے اوقات سست ہونا
**علامات:**
- ڈیپلائمنٹس بہت دیر لے رہے ہیں
- ڈیپلائمنٹ کے دوران ٹائم آؤٹ

**حل:**
```bash
# 1. تیز تکرار کے لیے مخصوص خدمات تعینات کریں
azd deploy --service web
azd deploy --service api

# 2. جب انفراسٹرکچر میں تبدیلی نہ ہو تو صرف کوڈ کی تعیناتی استعمال کریں
azd deploy  # azd up کے مقابلے میں تیز

# 3. بلڈ کے عمل کو بہتر بنائیں
# package.json میں:
"scripts": {
  "build": "webpack --mode=production --optimize-minimize"
}

# 4. وسائل کے مقامات چیک کریں (ایک ہی ریجن استعمال کریں)
azd config set defaults.location eastus2
```

### مسئلہ: ایپلیکیشن کی کارکردگی کے مسائل
**علامات:**
- ردِ عمل کے اوقات سست ہیں
- وسائل کا زیادہ استعمال

**حل:**
```bash
# 1. وسائل کو بڑھائیں
# main.parameters.json میں SKU کو اپ ڈیٹ کریں:
"appServiceSku": {
  "value": "S2"  // Scale up from B1
}

# 2. Application Insights کی نگرانی کو فعال کریں
azd monitor --overview

# 3. Azure میں ایپلیکیشن لاگز چیک کریں
az webapp log tail --name myapp --resource-group myrg
# یا Container Apps کے لیے:
az containerapp logs show --name myapp --resource-group myrg --follow

# 4. کیشنگ لاگو کریں
# اپنے انفراسٹرکچر میں Redis کیش شامل کریں
```

## 🛠️ ٹربل شوٹنگ ٹولز اور کمانڈز

### ڈیبگ کمانڈز
```bash
# جامع ڈیبگنگ
export AZD_DEBUG=true
azd up --debug 2>&1 | tee debug.log

# azd کا ورژن چیک کریں
azd version

# موجودہ کنفیگریشن دیکھیں
azd config list

# کنیکٹیویٹی ٹیسٹ کریں
curl -v https://myapp.azurewebsites.net/health
```

### لاگ تجزیہ
```bash
# Azure CLI کے ذریعے ایپلیکیشن لاگز
az webapp log tail --name myapp --resource-group myrg

# azd کے ذریعے ایپلیکیشن کی نگرانی
azd monitor --logs
azd monitor --live

# Azure وسائل کے لاگز
az monitor activity-log list --resource-group myrg --start-time 2024-01-01 --max-events 50

# کنٹینر لاگز (Container Apps کے لیے)
az containerapp logs show --name myapp --resource-group myrg --follow
```

### وسائل کی تفتیش
```bash
# تمام وسائل کی فہرست بنائیں
az resource list --resource-group myrg -o table

# وسائل کی حیثیت چیک کریں
az webapp show --name myapp --resource-group myrg --query state

# نیٹ ورک کی تشخیص
az network watcher test-connectivity --source-resource myvm --dest-address myapp.azurewebsites.net --dest-port 443
```

## 🆘 اضافی مدد حاصل کرنا

### جب اسکلیشن کی ضرورت ہو
- تمام حل آزمانے کے بعد بھی تصدیقی مسائل برقرار ہوں
- Azure سروسز کے ساتھ انفراسٹرکچر مسائل
- بلنگ یا سبسکرپشن سے متعلق مسائل
- سیکیورٹی خدشات یا واقعات

### سپورٹ چینلز
```bash
# 1. Azure سروس ہیلتھ چیک کریں
az rest --method get --uri "https://management.azure.com/subscriptions/{subscription-id}/providers/Microsoft.ResourceHealth/availabilityStatuses?api-version=2020-05-01"

# 2. Azure سپورٹ ٹکٹ بنائیں
# پر جائیں: https://portal.azure.com -> مدد + سپورٹ

# 3. کمیونٹی وسائل
# - Stack Overflow: azure-developer-cli ٹیگ
# - GitHub ایشوز: https://github.com/Azure/azure-dev/issues
# - Microsoft سوالات و جوابات: https://learn.microsoft.com/en-us/answers/
```

### جمع کرنے کے لیے معلومات
سپورٹ سے رابطہ کرنے سے پہلے، یہ معلومات جمع کریں:
- `azd version` کا آؤٹ پٹ
- `azd config list` کا آؤٹ پٹ
- `azd show` آؤٹ پٹ (موجودہ ڈیپلائمنٹ کی حالت)
- خرابی کے پیغامات (مکمل متن)
- مسئلہ دوبارہ پیدا کرنے کے اقدامات
- ماحول کی تفصیلات (`azd env show`)
- مسئلے کے شروع ہونے کا ٹائم لائن

### لاگ کلیکشن اسکرپٹ
```bash
#!/بن/باش
# ڈیبگ-معلومات-جمع-کریں.sh

echo "Collecting azd debug information..."
mkdir -p debug-logs

echo "System Information:" > debug-logs/system-info.txt
azd version >> debug-logs/system-info.txt
az --version >> debug-logs/system-info.txt

echo "Configuration:" > debug-logs/config.txt
azd config list >> debug-logs/config.txt
azd env show >> debug-logs/config.txt
azd env get-values >> debug-logs/config.txt

echo "Current deployment status:" > debug-logs/status.txt
azd show >> debug-logs/status.txt

echo "Debug information collected in debug-logs/"
```

## 📊 مسئلے کی روک تھام

### پری-ڈیپلائمنٹ چیک لسٹ
```bash
# تصدیق کی جانچ کریں
az account show

# کوٹے اور حدود چیک کریں
az vm list-usage --location eastus2

# ٹیمپلیٹس کی توثیق کریں
az bicep build --file infra/main.bicep

# پہلے مقامی طور پر جانچ کریں
npm run build
npm run test

# ڈرائی رن تعیناتیوں کا استعمال کریں
azd provision --preview
```

### مانیٹرنگ سیٹ اپ
```bash
# Application Insights کو فعال کریں
# main.bicep میں شامل کریں:
resource appInsights 'Microsoft.Insights/components@2020-02-02' = {
  // ... configuration
}

# الارٹس مرتب کریں
az monitor metrics alert create \
  --name "High CPU Usage" \
  --resource-group myrg \
  --scopes /subscriptions/{id}/resourceGroups/myrg/providers/Microsoft.Web/sites/myapp \
  --condition "avg Percentage CPU > 80"
```

### باقاعدہ مینٹیننس
```bash
# ہفتہ وار صحت کے معائنے
./scripts/health-check.sh

# ماہانہ لاگت کا جائزہ
az consumption usage list --billing-period-name 202401

# سہ ماہی سیکیورٹی کا جائزہ
az security assessment list --resource-group myrg
```

## متعلقہ وسائل

- [ڈیبگنگ گائیڈ](debugging.md) - اعلی درجے کی ڈیبگنگ تکنیکس
- [ریسورس پروویژننگ](../chapter-04-infrastructure/provisioning.md) - انفراسٹرکچر ٹربل شوٹنگ
- [کیپیسِٹی پلاننگ](../chapter-06-pre-deployment/capacity-planning.md) - وسائل کی منصوبہ بندی کے رہنما اصول
- [SKU انتخاب](../chapter-06-pre-deployment/sku-selection.md) - سروس ٹئیر کی سفارشات

---

**ٹپ**: اس گائیڈ کو بک مارک رکھیں اور جب بھی آپ کو مسائل کا سامنا ہو تو اس کا حوالہ دیں۔ زیادہ تر مسائل پہلے بھی دیکھے جا چکے ہوتے ہیں اور ان کے ثابت شدہ حل موجود ہیں!

---

**نیویگیشن**
- **پچھلا سبق**: [ریسورس پروویژننگ](../chapter-04-infrastructure/provisioning.md)
- **اگلا سبق**: [ڈیبگنگ گائیڈ](debugging.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
ردِ ذمہ داری:
یہ دستاویز AI ترجمہ سروس [Co-op Translator](https://github.com/Azure/co-op-translator) کے ذریعے ترجمہ کی گئی ہے۔ اگرچہ ہم درستگی کی پوری کوشش کرتے ہیں، براہِ کرم نوٹ کریں کہ خودکار تراجم میں غلطیاں یا نادرستیاں ہو سکتی ہیں۔ اصل دستاویز کو اس کی مادری زبان میں ہی مستند ماخذ سمجھا جانا چاہیے۔ اہم معلومات کے لیے پیشہ ور انسانی ترجمے کی سفارش کی جاتی ہے۔ اس ترجمے کے استعمال سے پیداہونے والی کسی بھی غلط فہمی یا غلط تعبیر کے لیے ہم ذمہ دار نہیں ہیں۔
<!-- CO-OP TRANSLATOR DISCLAIMER END -->